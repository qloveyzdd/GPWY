---
phase: 02-manual-refresh-cache
plan: "02-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/refresh/refresh-types.ts
  - src/lib/refresh/refresh-store.ts
  - tests/refresh/refresh-store.test.ts
autonomous: true
requirements: [REFR-02, REFR-03, REFR-05]
must_haves:
  truths:
    - "D-02-02: daily_bars stores ts_code, trade_date, open, high, low, close, and vol."
    - "D-02-05: SQLite state is the concurrency guard for refresh jobs."
    - "D-02-08: failed jobs are not treated as the latest successful cache."
    - "Refresh store tests cover create, running lock, success, failure, and cache reads."
  artifacts:
    - path: "src/lib/refresh/refresh-store.ts"
      provides: "SQLite schema and data access layer for refresh jobs and cache tables"
    - path: "src/lib/refresh/refresh-types.ts"
      provides: "Refresh job, status, stock basic, and daily bar types"
---

## Phase Goal

**As a** maintainer, **I want** a durable refresh/cache store, **so that** later refresh APIs and screening logic have a reliable source of job state and market data.

<objective>
Create the SQLite schema and server-only data access layer for manual refresh jobs, stock basics, and daily bars.
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-manual-refresh-cache/02-CONTEXT.md
@.planning/phases/02-manual-refresh-cache/02-RESEARCH.md
@.planning/phases/01-tushare-data-foundation/01-03-SUMMARY.md

<interfaces>
- Reuse the better-sqlite3 server-only pattern from `src/lib/validation-store.ts`.
- Keep validation snapshots untouched.
- New code should live under `src/lib/refresh/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add refresh store tests and types</name>
  <files>tests/refresh/refresh-store.test.ts, src/lib/refresh/refresh-types.ts</files>
  <behavior>
    - Tests use a temp SQLite path via environment variable.
    - Types model `running`, `succeeded`, and `failed` refresh jobs.
    - Tests describe the lock and latest-success semantics before implementation.
  </behavior>
  <action>Add failing tests for refresh job creation, duplicate running-job prevention, success/failure transitions, stock basic upsert, daily bar upsert, and latest successful cache reads.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/refresh-store.test.ts</automated>
  </verify>
  <done>Tests fail for missing refresh store behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SQLite refresh schema and DAL</name>
  <files>src/lib/refresh/refresh-store.ts, tests/refresh/refresh-store.test.ts</files>
  <behavior>
    - `startRefreshJob()` atomically returns a new running job or the existing running job.
    - `completeRefreshJob()` marks a job succeeded with counts.
    - `failRefreshJob()` marks a job failed with sanitized error summary.
    - Cache writes are associated with a refresh job id.
    - Latest cache readers only use the latest succeeded job.
  </behavior>
  <action>Implement schema initialization, job lifecycle helpers, active-job lookup, latest-status lookup, stock basic upsert, daily bar upsert, and latest successful cache readers.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/refresh-store.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <done>Refresh store tests pass and no TypeScript errors remain.</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/refresh/refresh-store.test.ts`
- `D:\NodeJS\npm.cmd run typecheck`
</verification>

<success_criteria>
- SQLite tables are created automatically.
- Concurrent refresh start attempts cannot create two running jobs.
- Failed jobs are stored but excluded from latest successful cache reads.
- Stock basics and daily bars can be written and read by refresh job.
</success_criteria>

<output>
Create `.planning/phases/02-manual-refresh-cache/02-01-SUMMARY.md` when done.
</output>
