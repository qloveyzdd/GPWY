---
phase: 11-distribution-comparison-experience
plan: "11-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/results/results-table.tsx
  - tests/ui/results-table.test.tsx
autonomous: true
requirements: [UI-05]
user_setup: []
must_haves:
  truths:
    - "D-11-01: 继续使用当前结果行下方 inline 展开模式，表格点击行展开交互不改变。"
    - "D-11-15/D-11-16/D-11-17/D-11-18: 表格删除筹码峰字段、筹码峰排序、筹码状态徽标和额外查看按钮或 hover 提示。"
    - "UI-05: 结果表格删除筹码峰字段和筹码峰排序能力。"
  artifacts:
    - path: "src/components/results/results-table.tsx"
      provides: "Simplified results table with only price/trend columns and inline chart expansion"
    - path: "tests/ui/results-table.test.tsx"
      provides: "Regression tests for no chip peak table UI and preserved sorting/inline expansion"
  key_links:
    - from: "src/components/results/results-table.tsx"
      to: "src/components/charts/stock-kline-chart.tsx"
      via: "row click still renders inline stock detail chart below selected row"
---

<objective>
简化结果表格：删除筹码峰列、筹码峰状态展示和筹码峰排序入口，只保留股票基础字段、当前/高点排序、下跌幅度排序和现有点击行 inline 展开交互。

Purpose: Phase 11 的筹码信息应集中到详情区完整分布图中，表格只承担筛选结果扫视和排序入口，避免继续让“前三筹码峰”主导体验。
Output: 表格 UI 删除旧筹码峰消费，相关组件测试更新。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-distribution-comparison-experience/11-CONTEXT.md
@.planning/phases/11-distribution-comparison-experience/11-RESEARCH.md
@src/components/results/results-table.tsx
@src/components/status/status-workspace.tsx
@src/components/charts/stock-kline-chart.tsx
@src/lib/results/results-types.ts
@tests/ui/results-table.test.tsx
@tests/ui/status-workspace.test.tsx

<interfaces>
- `ResultsTable` props 仍是 `{ snapshot: ResultsSnapshot }`。
- `SortKey` 只允许 `"currentHighRatio" | "drawdownPct"`。
- 表格列数为 6：股票代码、名称、当前价、区间高点、当前/高点、下跌幅度。
- 展开行仍渲染 `<StockKlineChart tsCode={row.tsCode} />`，`colSpan` 与表格列数一致。
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-11-04: 删除筹码峰列时破坏行点击展开或键盘展开 | medium | 保留现有 row `onClick`、Enter/Space 处理和 `aria-expanded` 测试。 |
| T-11-05: 表格仍通过旧字段显示 blocked/failed/missing，造成状态信息分散 | low | 删除 `ChipPeakCell` 和 chip 状态 label，测试断言表格不出现这些文案。 |
| T-11-06: 移除排序 key 后留下不可达按钮或类型死分支 | low | `SortKey` 联合类型收窄，删除 `chipPeakPrice` 分支，typecheck 覆盖。 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 删除表格筹码峰列和筹码峰状态渲染</name>
  <files>src/components/results/results-table.tsx, tests/ui/results-table.test.tsx</files>
  <read_first>src/components/results/results-table.tsx, tests/ui/results-table.test.tsx, .planning/phases/11-distribution-comparison-experience/11-CONTEXT.md</read_first>
  <behavior>
    - 不显示“筹码峰价格”表头。
    - 不显示前三筹码峰价格/占比。
    - 不显示“可用/阻塞/失败/无数据”等筹码状态 badge。
    - 不新增“查看图表/分布”按钮或 hover 提示。
  </behavior>
  <action>在 `results-table.tsx` 删除 `ResultChipPeakState` import、`chipStateLabels`、`chipStateTone`、`ChipPeakCell`、相关 lucide icon import 和最后一列 `<TableCell>`。保留 `ResultRow` 类型，因为 backend 兼容字段仍存在。更新测试中的 “renders required stock result columns” 用例：断言只有 6 个业务列，且 queryByText("筹码峰价格")、queryByText("38.50" 作为筹码峰上下文)、queryByText("阻塞") 等表格筹码状态不存在；保留股票基础值断言。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/results-table.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 页面不渲染“筹码峰价格”
    - available row 不渲染 chipPeaks 的价格/percent 列表
    - blocked/failed/missing chip state 文案不在表格行内出现
    - `StatusWorkspace` 仍能渲染 `ResultsTable`
  </acceptance_criteria>
  <done>表格视觉层不再消费旧筹码峰字段。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 删除筹码峰排序入口并保留两个趋势排序</name>
  <files>src/components/results/results-table.tsx, tests/ui/results-table.test.tsx</files>
  <read_first>src/components/results/results-table.tsx, tests/ui/results-table.test.tsx</read_first>
  <behavior>
    - 默认仍按 current/high ratio 升序。
    - “下跌幅度”排序仍按默认降序。
    - 不存在 “按筹码峰价格排序” 按钮。
    - 不存在 `chipPeakPrice` sort key 分支。
  </behavior>
  <action>将 `SortKey` 改为 `"currentHighRatio" | "drawdownPct"`。删除 `sortValue()` 对 `chipPeakPrice` 的 null-last 特殊处理，排序直接读取数值字段；如保留 helper，必须只有两个 key。删除筹码峰 SortHeader。更新测试：删除“sorts by chip peak price”用例，新增断言 `queryByRole("button", { name: "按筹码峰价格排序" })` 为 null；保留 current/high ratio 和 drawdown 排序用例。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/results-table.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg "chipPeakPrice|筹码峰价格|按筹码峰价格排序" src/components/results/results-table.tsx` 无结果
    - current/high ratio 默认升序测试通过
    - drawdown 默认降序测试通过
    - unavailable chip rows 不再影响任何表格排序逻辑
  </acceptance_criteria>
  <done>表格排序入口与 Phase 11 简化目标一致。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 调整 inline 展开行列宽并验证交互不退化</name>
  <files>src/components/results/results-table.tsx, tests/ui/results-table.test.tsx</files>
  <read_first>src/components/results/results-table.tsx, tests/ui/results-table.test.tsx</read_first>
  <behavior>
    - 展开行仍紧跟被点击结果行。
    - 鼠标点击同一行可打开/关闭详情。
    - Enter/Space 键盘交互仍可用。
    - 展开行 `colSpan` 与 6 列表格匹配。
  </behavior>
  <action>将展开行 `<TableCell colSpan={7}>` 改为 `colSpan={6}`。保留现有 `aria-selected`、`aria-expanded`、`tabIndex` 和 keyboard handler。补充或更新测试断言展开行仍在目标行 `nextElementSibling`，并可通过点击关闭；如测试环境方便，增加 Space/Enter 键盘展开断言。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/results-table.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 展开详情仍渲染 `StockKlineChart`
    - `stock-chart-row-${tsCode}` 仍紧跟目标行
    - 点击同一行第二次关闭详情
    - 表格 DOM 无多余空列或 colSpan 不匹配
  </acceptance_criteria>
  <done>表格简化不破坏现有 inline 详情入口。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/ui/results-table.test.tsx tests/ui/status-workspace.test.tsx`
- `D:\NodeJS\npm.cmd run typecheck`
- `D:\NodeJS\npm.cmd run lint -- src/components/results tests/ui/results-table.test.tsx`
</verification>

<success_criteria>
- 结果表格不再显示任何筹码峰字段、筹码状态或筹码峰排序入口。
- 表格只保留 current/high ratio 和 drawdown 两个排序入口。
- 行点击/键盘展开详情仍正常工作。
- `StatusWorkspace` 集成不退化。
</success_criteria>

<output>
完成后创建 `.planning/phases/11-distribution-comparison-experience/11-02-SUMMARY.md`。
</output>
