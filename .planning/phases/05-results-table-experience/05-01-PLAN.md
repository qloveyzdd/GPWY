---
phase: 05-results-table-experience
plan: "05-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/results/results-types.ts
  - src/lib/results/results-snapshot.ts
  - src/components/results/results-table.tsx
  - src/components/status/status-workspace.tsx
  - src/app/page.tsx
  - tests/results/results-snapshot.test.ts
  - tests/ui/results-table.test.tsx
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "D-05-01: 表格默认按当前价/区间高点比例升序排列。"
    - "D-05-03: 筹码峰不可用不能隐藏已入选股票。"
    - "Phase 5 读取已持久化的筛选和筹码数据，不在 UI 中重新计算筛选算法。"
  artifacts:
    - path: "src/lib/results/results-snapshot.ts"
      provides: "服务端最新结果快照，负责连接筛选结果和同一 screeningRunId 的筹码峰结果"
    - path: "src/components/results/results-table.tsx"
      provides: "展示必需列的结果表格组件"
---

## Phase Goal

**As a** user, **I want** to see the latest persisted screening results in a table, **so that** I can quickly inspect the matched stocks and their chip peak status.

<objective>
创建最新结果快照层，并在受保护的首页工作区中渲染必需结果列。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-results-table-experience/05-CONTEXT.md
@.planning/phases/05-results-table-experience/05-RESEARCH.md
@.planning/phases/03-downtrend-screening-engine/03-03-SUMMARY.md
@.planning/phases/04-chip-peak-integration/04-02-SUMMARY.md

<interfaces>
- Use `readLatestScreeningRun()` and `readLatestScreeningResults()` as the screening source.
- Use `readLatestChipPeakRun()` and `readLatestChipPeakResults()` as the chip peak source.
- Join chip peak rows only when `chipPeakRun.screeningRunId === screeningRun.id`.
- Keep `StatusWorkspace` as the first protected screen and pass a typed snapshot into it from `src/app/page.tsx`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add latest results snapshot tests</name>
  <files>tests/results/results-snapshot.test.ts</files>
  <behavior>
    - No screening run returns a page-level `unavailable` snapshot.
    - A successful screening run with no rows returns an `empty` snapshot.
    - Screening rows with a matching chip run return rows with numeric chip peak prices.
    - Missing, blocked, failed, or stale chip peak rows keep the stock visible with explicit row-level chip state.
    - Ready rows default to `currentHighRatio` ascending.
  </behavior>
  <action>Add offline tests with temporary SQLite fixtures and persisted screening/chip records.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/results/results-snapshot.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the results snapshot layer</name>
  <files>src/lib/results/results-types.ts, src/lib/results/results-snapshot.ts</files>
  <behavior>
    - Snapshot status is one of `ready`, `empty`, or `unavailable`.
    - Row fields include stock code, name, current price, interval high, current/high ratio, drawdown, and chip peak price/state.
    - Chip peak state is one of `available`, `blocked`, `failed`, or `missing`.
    - The snapshot never joins chip peak data from a different screening run.
    - The helper does not call Tushare/tinyshare and does not recompute MA or downtrend algorithms.
  </behavior>
  <action>Create typed result rows and a `readLatestResultsSnapshot()` server helper.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/results/results-snapshot.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render the required results table columns</name>
  <files>src/components/results/results-table.tsx, src/components/status/status-workspace.tsx, src/app/page.tsx, tests/ui/results-table.test.tsx</files>
  <behavior>
    - Table columns show stock code, name, current price, interval high, current/high ratio, drawdown, and chip peak price.
    - Numeric values are formatted as prices or percentages rather than raw decimals.
    - Row-level chip unavailable states render an explicit marker, not `0` and not blank.
    - `src/app/page.tsx` reads the snapshot server-side and passes it to `StatusWorkspace`.
  </behavior>
  <action>Add a focused `ResultsTable` component under `src/components/results/` and wire it into the existing workspace.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/results/results-snapshot.test.ts tests/ui/results-table.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- The latest persisted screening results render on the protected home page.
- The required columns from UI-02 are present and consistently formatted.
- Chip peak unavailable states remain visible at row level.
- Default row order follows D-05-01.
</success_criteria>
