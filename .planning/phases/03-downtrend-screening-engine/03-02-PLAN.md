---
phase: 03-downtrend-screening-engine
plan: "03-02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/screening/downtrend-screen.ts
  - tests/screening/downtrend-screen.test.ts
autonomous: true
requirements: [SCRN-02, SCRN-03, SCRN-04, SCRN-05, SCRN-06, SCRN-07, SCRN-08]
---

## Phase Goal

**As a** maintainer, **I want** a pure stock evaluator, **so that** each stock can be accepted or skipped with explainable values.

<objective>
Implement swing-high detection, 85% threshold screening, and per-stock explainable result calculation.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add downtrend evaluator tests</name>
  <files>tests/screening/downtrend-screen.test.ts</files>
  <behavior>
    - Latest strict local swing high wins when present.
    - Missing local swing high falls back to latest 60-day highest high.
    - Candidate must satisfy MA20 < MA60, negative MA20 slope, and current close <= high * 0.85.
    - Insufficient bars produce a skipped result, not a false match.
  </behavior>
  <action>Add focused tests using generated 60-bar fixtures.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/screening/downtrend-screen.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement downtrend evaluator</name>
  <files>src/lib/screening/downtrend-screen.ts</files>
  <behavior>
    - Evaluate only the latest 60 bars by trade date.
    - Persist-ready result includes current price, interval high, ratio, drawdown, MA20, MA60, MA20 slope, latest trade date, and high source.
  </behavior>
  <action>Use 03-01 indicator helpers and keep evaluator pure.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/screening/indicators.test.ts tests/screening/downtrend-screen.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Swing high and fallback logic are tested.
- 85% threshold boundary is tested.
- Accepted results expose all numeric values required by later UI/chart phases.
</success_criteria>
