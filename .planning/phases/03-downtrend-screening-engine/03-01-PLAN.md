---
phase: 03-downtrend-screening-engine
plan: "03-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/screening/screening-types.ts
  - src/lib/screening/indicators.ts
  - tests/screening/indicators.test.ts
autonomous: true
requirements: [SCRN-01, SCRN-02, SCRN-03, SCRN-08]
---

## Phase Goal

**As a** maintainer, **I want** pure moving-average and slope helpers, **so that** the downtrend screen can be tested independently from SQLite.

<objective>
Implement MA20/MA60 helpers and MA20 5-point negative slope detection.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add indicator tests</name>
  <files>tests/screening/indicators.test.ts</files>
  <behavior>
    - Moving average returns no value until enough close prices exist.
    - Latest MA20 and MA60 are calculated from chronological close prices.
    - MA20 slope is negative when latest MA20 is below the first point of the latest 5 MA20 points.
    - Insufficient slope data returns false.
  </behavior>
  <action>Add tests for moving averages and slope without touching persistence.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/screening/indicators.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement indicator helpers</name>
  <files>src/lib/screening/screening-types.ts, src/lib/screening/indicators.ts</files>
  <behavior>
    - Sort daily bars ascending by `tradeDate`.
    - Calculate simple moving-average series for arbitrary periods.
    - Return latest MA values and slope decisions as typed results.
  </behavior>
  <action>Implement small pure functions with no database or provider coupling.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/screening/indicators.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- MA20 and MA60 helpers are tested.
- MA20 5-point slope logic is tested.
- No provider/database code is introduced in this plan.
</success_criteria>
