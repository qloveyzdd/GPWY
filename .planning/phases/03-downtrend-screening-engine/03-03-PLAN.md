---
phase: 03-downtrend-screening-engine
plan: "03-03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/lib/screening/screening-store.ts
  - src/lib/screening/screening-runner.ts
  - tests/screening/screening-store.test.ts
  - tests/screening/screening-runner.test.ts
autonomous: true
requirements: [SCRN-01, SCRN-02, SCRN-03, SCRN-04, SCRN-05, SCRN-06, SCRN-07, SCRN-08]
---

## Phase Goal

**As a** maintainer, **I want** screening results persisted from the latest refresh cache, **so that** later chip/table/chart phases can read stable explainable results.

<objective>
Read latest refresh cache, run the pure evaluator per stock, and persist selected screening results.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add screening persistence tests</name>
  <files>tests/screening/screening-store.test.ts, tests/screening/screening-runner.test.ts</files>
  <behavior>
    - Store can write and read latest screening run and results.
    - Runner reads latest successful refresh cache.
    - Runner persists only accepted stocks and records skipped count for insufficient data.
  </behavior>
  <action>Add tests with temporary SQLite files and synthetic bars.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/screening/screening-store.test.ts tests/screening/screening-runner.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement screening store and runner</name>
  <files>src/lib/screening/screening-store.ts, src/lib/screening/screening-runner.ts</files>
  <behavior>
    - Use the same SQLite database path as refresh cache.
    - Reference the source refresh job id.
    - Persist stock code/name and all explainable computed values.
    - Throw a clear error when no successful refresh cache exists.
  </behavior>
  <action>Implement store schema, latest-result readers, and cache-driven runner.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/screening/indicators.test.ts tests/screening/downtrend-screen.test.ts tests/screening/screening-store.test.ts tests/screening/screening-runner.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run verify</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Screening results are persisted with source refresh job id.
- Latest screening results can be read by later phases.
- Full project verification passes.
