---
phase: 12-decay-based-chip-distribution-model
plan: "12-02"
type: execute
wave: 1
depends_on: ["12-01"]
files_modified:
  - src/lib/chip/chip-types.ts
  - src/lib/chip/chip-model.ts
  - tests/chip/chip-model.test.ts
autonomous: true
requirements: [DATA-12, CMOD-01, CMOD-02, CMOD-03, CMOD-04, CMOD-05, VAL-01]
user_setup: []
must_haves:
  truths:
    - "D-12-01/D-12-02/D-12-03: 每日新增筹码默认使用三角分布，中心优先为成交均价，且必须限制在当日 [low, high] 区间内。"
    - "D-12-04: 旧筹码衰减量由 turnoverRate * decayCoefficient 决定，新筹码权重来自被衰减掉的旧筹码权重，每日处理后归一化。"
    - "D-12-05/D-12-06: 种子分布和日线必须处于同一复权价格口径，输出必须标记为计算分布/模型输出。"
    - "D-12-15/D-12-16: 算法本身必须能独立计算任意 target date，latest 和 previous 失败互不影响。"
  artifacts:
    - path: "src/lib/chip/chip-model.ts"
      provides: "pure decay-based chip distribution algorithm"
    - path: "src/lib/chip/chip-types.ts"
      provides: "model version, coefficient, model input and model output types"
    - path: "tests/chip/chip-model.test.ts"
      provides: "algorithm tests for coefficient, missing data, adjustment, and dual target behavior"
  key_links:
    - from: "src/lib/chip/chip-model.ts"
      to: "src/lib/refresh/market-data-reader.ts"
      via: "consumes adjusted model bars produced by plan 12-01"
    - from: "src/lib/chip/chip-model-runner.ts"
      to: "src/lib/chip/chip-model.ts"
      via: "later runner executes pure algorithm and stores output"
---

<objective>
实现不依赖数据库的衰减筹码分布纯算法。输入为 60 个交易日前官方种子分布、复权一致交易序列和衰减系数；输出为目标日计算分布或结构化不可用原因。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/12-decay-based-chip-distribution-model/12-CONTEXT.md
@.planning/phases/12-decay-based-chip-distribution-model/12-RESEARCH.md
@.planning/phases/12-decay-based-chip-distribution-model/12-PATTERNS.md
@src/lib/chip/chip-types.ts
@src/lib/refresh/market-data-reader.ts

<interfaces>
- `CHIP_MODEL_VERSION = "decay-triangle-v1"` 或等价常量。
- `ChipDecayCoefficient` 固定联合类型：`0.3 | 0.5 | 0.8 | 1.0 | 1.2 | 1.5 | 2.0`。
- `DEFAULT_CHIP_DECAY_COEFFICIENT = 0.5`。
- `calculateDecayChipDistribution(input)` 返回 `succeeded` 或 `unavailable`。
- 输出 levels 的 percent 总和约等于 100，误差在测试定义的 epsilon 内。
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-12-03: 模型把全部新筹码均匀铺满区间，导致柱状图过粗或不符合用户预期 | medium | 默认三角核，价格档位自适应细分，上限控制性能 |
| T-12-04: 高换手率和大系数导致旧筹码变成负数 | high | 单日衰减率 clamp 到 `[0, 1]` |
| T-12-05: 多日浮点误差累积，percent 总和漂移 | medium | 每日归一化，测试 epsilon |
| T-12-06: 除权后种子价格轴与目标日 K 线错位 | high | 输入要求 seed levels 已转换到目标复权口径；测试覆盖 price ratio |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义模型类型、版本和系数集合</name>
  <files>src/lib/chip/chip-types.ts, src/lib/chip/chip-model.ts, tests/chip/chip-model.test.ts</files>
  <read_first>src/lib/chip/chip-types.ts, .planning/phases/12-decay-based-chip-distribution-model/12-CONTEXT.md</read_first>
  <behavior>
    - 固定系数集合只包含用户确认的 7 个值。
    - 默认系数为 0.5。
    - 模型版本作为缓存 key 的组成部分。
    - 类型区分 official distribution 和 calculated distribution。
  </behavior>
  <action>新增模型相关类型和常量；如果类型放在 `chip-types.ts` 会导致文件过大，可在 `chip-model.ts` 中导出并从 `chip-types.ts` re-export，但必须保持后续 store/UI 可导入。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-model.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 非固定系数在 TypeScript 或运行时校验中被拒绝。
    - 默认系数测试断言为 0.5。
    - 模型输出类型包含 `modelVersion`、`decayCoefficient`、`seedTradeDate`、`targetTradeDate`。
  </acceptance_criteria>
  <done>模型的公共契约可被 store、runner 和 UI 复用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现单日衰减和三角分布新增筹码</name>
  <files>src/lib/chip/chip-model.ts, tests/chip/chip-model.test.ts</files>
  <read_first>src/lib/chip/chip-model.ts, tests/chip/chip-model.test.ts</read_first>
  <behavior>
    - 单日衰减率为 `clamp(turnoverRate / 100 * decayCoefficient, 0, 1)`。
    - 旧筹码按衰减率减少。
    - 被减少的总权重按三角核分配到当日价格区间。
    - 高低价相同或价格区间极窄时仍能稳定归一化。
  </behavior>
  <action>实现 `applyChipDecayDay()` 或等价纯函数。价格档位使用自适应步长，目标是比当前 60 日可视范围柱数更细，但设置上限避免生成过多 levels。三角核权重按离中心越近权重越高，中心超出区间时 clamp 到 `[low, high]`。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 新增筹码没有落在当日 `[low, high]` 外。
    - `0.3` 比 `1.5` 保留更多旧高位筹码。
    - 每日处理后 percent 总和约等于 100。
    - 高低价相同的 bar 不会产生 NaN 或空分布。
  </acceptance_criteria>
  <done>单日转换公式可解释且可测试。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现从种子日到目标日的完整推演</name>
  <files>src/lib/chip/chip-model.ts, tests/chip/chip-model.test.ts</files>
  <read_first>src/lib/chip/chip-model.ts, tests/chip/chip-model.test.ts, src/lib/refresh/market-data-reader.ts</read_first>
  <behavior>
    - 从 seed distribution 开始，按 tradeDate 升序逐日处理。
    - 支持 latest 和 previous 调用同一函数。
    - 缺 seed、缺 bar、缺 turnover、缺 adjustment 时返回结构化 unavailable。
    - 不读数据库，不请求网络。
  </behavior>
  <action>实现 `calculateDecayChipDistribution()`。输入中显式携带 seedTradeDate、targetTradeDate、seed levels、model bars、decayCoefficient、modelVersion。函数校验模型 bars 覆盖从 seed 后一个交易日到 target 的区间；如果 target 等于 seed，直接归一化返回 seed snapshot。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-model.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 缺失 seed levels 返回 `missing_seed_distribution`。
    - 缺失中间交易日返回 `missing_trade_data`。
    - latest 和 previous 使用不同 targetDate 时输出互不污染。
    - 输出 levels 按 price 升序稳定排序。
  </acceptance_criteria>
  <done>纯算法能生成可缓存的目标日计算分布。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-model.test.ts`
- `D:\NodeJS\npm.cmd run typecheck`
- `D:\NodeJS\npm.cmd run lint -- src/lib/chip tests/chip`
</verification>

<success_criteria>
- 固定衰减系数集合和默认 0.5 已定义。
- 旧筹码衰减、新筹码三角分配、每日归一化都有单元测试。
- 复权口径、缺失输入、双目标日期计算都有测试。
- 算法层不依赖数据库和 provider。
</success_criteria>

<output>
完成后创建 `.planning/phases/12-decay-based-chip-distribution-model/12-02-SUMMARY.md`。
</output>
