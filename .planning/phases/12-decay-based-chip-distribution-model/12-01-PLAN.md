---
phase: 12-decay-based-chip-distribution-model
plan: "12-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tushare/endpoints.ts
  - src/lib/refresh/market-data-types.ts
  - src/lib/refresh/fetch-refresh-data.ts
  - src/lib/refresh/market-data-store.ts
  - src/lib/refresh/market-data-reader.ts
  - tests/refresh/fetch-refresh-data.test.ts
  - tests/refresh/market-data-store.test.ts
  - tests/refresh/market-data-reader.test.ts
  - tests/validation/basic-data.test.ts
autonomous: true
requirements: [DATA-11, DATA-12, CMOD-02, CMOD-03, CMOD-04, VAL-01]
user_setup: []
must_haves:
  truths:
    - "D-12-02/D-12-03/D-12-05: 模型输入必须包含复权一致的 OHLC、成交均价和价格区间；成交均价优先来自 amount/vol，缺失时才允许典型价 fallback。"
    - "D-12-04/D-12-19: 换手率缺失必须是结构化不可用原因，不能用 vol、固定常数或静默近似继续。"
    - "D-12-07/D-12-08/D-12-09/D-12-10: 固定衰减系数集合和默认 0.5 要在类型层表达，供后续算法、缓存和 UI 共用。"
    - "DATA-11/CMOD-02/VAL-01: 日线、成交额、换手率和复权因子必须来自同一 market generation。"
  artifacts:
    - path: "src/lib/tushare/endpoints.ts"
      provides: "daily amount field and daily_basic turnover endpoint"
    - path: "src/lib/refresh/market-data-types.ts"
      provides: "raw daily quote amount, daily basic turnover, and model input DTO types"
    - path: "src/lib/refresh/market-data-reader.ts"
      provides: "adjusted model bars for chip calculation"
  key_links:
    - from: "src/lib/refresh/fetch-refresh-data.ts"
      to: "src/lib/tushare/endpoints.ts"
      via: "fetches daily, adj_factor, and daily_basic per trade date through shared scheduler"
    - from: "src/lib/chip/chip-model.ts"
      to: "src/lib/refresh/market-data-reader.ts"
      via: "later plans consume adjusted model bars instead of raw daily rows"
---

<objective>
补齐衰减筹码模型所需的数据输入：在现有 market generation 缓存中增加成交额和换手率，并提供复权一致的模型输入读取函数。该计划不实现筹码算法，只解决“数据是否足够可靠”的前置问题。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/12-decay-based-chip-distribution-model/12-CONTEXT.md
@.planning/phases/12-decay-based-chip-distribution-model/12-RESEARCH.md
@.planning/phases/12-decay-based-chip-distribution-model/12-PATTERNS.md
@src/lib/tushare/endpoints.ts
@src/lib/refresh/market-data-types.ts
@src/lib/refresh/fetch-refresh-data.ts
@src/lib/refresh/market-data-store.ts
@src/lib/refresh/market-data-reader.ts
@tests/refresh/fetch-refresh-data.test.ts
@tests/refresh/market-data-store.test.ts
@tests/refresh/market-data-reader.test.ts

<interfaces>
- `RawDailyQuoteRecord` 增加 `amount`，单位保持 Tushare `daily.amount` 原始单位。
- 新增 daily basic 类型，至少包含 `tsCode`、`tradeDate`、`turnoverRate`、`turnoverRateFreeFloat`。
- `TUSHARE_ENDPOINTS.daily` 增加 `amount` 字段。
- 新增 `TUSHARE_ENDPOINTS.dailyBasic`，字段包含 `ts_code`、`trade_date`、`turnover_rate`、`turnover_rate_f`。
- 新增或扩展 reader，返回给模型使用的 adjusted bar：`open/high/low/close/averagePrice/turnoverRate/vol/amount/adjFactor`。
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-12-01: 用成交量代替换手率，导致衰减速度完全失真 | high | 新增 daily_basic 缓存；缺失换手率直接不可用 |
| T-12-02: 成交均价与 OHLC 不在同一复权口径 | high | `amount/vol` 得到未复权均价后乘以与 OHLC 相同的复权 ratio |
| T-12-03: 扩表破坏已有 SQLite 数据库 | medium | `ensureSchema` 使用兼容迁移，已有行补 nullable/default，测试覆盖旧表升级 |
| T-12-04: daily_basic 权限不可用导致整个刷新失败 | medium | market item status 记录失败；模型阶段按缺失原因处理，不影响基础筛选 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 Tushare 端点和市场数据类型</name>
  <files>src/lib/tushare/endpoints.ts, src/lib/refresh/market-data-types.ts, src/lib/refresh/fetch-refresh-data.ts, tests/refresh/fetch-refresh-data.test.ts</files>
  <read_first>src/lib/tushare/endpoints.ts, src/lib/refresh/fetch-refresh-data.ts, tests/refresh/fetch-refresh-data.test.ts</read_first>
  <behavior>
    - `daily` 请求增加 `amount`，并在 mapper 中校验为 number。
    - 新增 `daily_basic` 请求和 mapper。
    - 换手率字段优先保留 `turnover_rate_f`，同时保留 `turnover_rate` 作为 fallback 输入。
    - 不改变 stock_basic、adj_factor、trade_cal 的行为。
  </behavior>
  <action>添加 `TUSHARE_ENDPOINTS.dailyBasic`；扩展 `RawDailyQuoteRecord` 和 fetch result；新增 `fetchDailyBasicForDate()`。在 `fetchRefreshData()` 和按日 bootstrap/incremental 路径中，将 daily、adj_factor、daily_basic 同一 tradeDate 一起拉取并返回。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/fetch-refresh-data.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 测试断言 `daily` fields 包含 `amount`。
    - 测试断言 `daily_basic` fields 包含 `turnover_rate` 和 `turnover_rate_f`。
    - mapper 对缺失 `amount` 或非法换手率有明确错误。
    - 现有日历和 adj_factor 测试仍通过。
  </acceptance_criteria>
  <done>项目能从 provider 层表达成交额和换手率来源。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 缓存成交额和换手率</name>
  <files>src/lib/refresh/market-data-store.ts, src/lib/refresh/market-data-types.ts, tests/refresh/market-data-store.test.ts</files>
  <read_first>src/lib/refresh/market-data-store.ts, tests/refresh/market-data-store.test.ts</read_first>
  <behavior>
    - `market_daily_quotes` 增加 `amount`。
    - 新增 `market_daily_basics` 或等价独立表保存换手率。
    - schema migration 必须兼容已有数据库。
    - 读写函数按 generation 隔离。
  </behavior>
  <action>为 store 增加 `upsertMarketDailyBasics()`、`readMarketDailyBasics()`，扩展 `upsertMarketDailyQuotes()` 和 `readMarketDailyQuotes()` 处理 `amount`。如果已有表缺列，使用安全 `alter table` 增列；测试覆盖旧 schema 迁移后可读写。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/market-data-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 同一 generation 下 daily quote 能读回 `amount`。
    - daily basic 能按 generation、股票、交易日读回。
    - building generation 清理不遗留 daily basic 数据。
    - 旧测试数据没有 daily basic 时不会破坏非模型读取路径。
  </acceptance_criteria>
  <done>市场缓存具备模型需要的原始数据存储能力。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 提供复权一致模型输入 reader</name>
  <files>src/lib/refresh/market-data-reader.ts, tests/refresh/market-data-reader.test.ts</files>
  <read_first>src/lib/refresh/market-data-reader.ts, tests/refresh/market-data-reader.test.ts, .planning/phases/12-decay-based-chip-distribution-model/12-CONTEXT.md</read_first>
  <behavior>
    - 返回从 seed 日到 target 日的连续可用交易序列。
    - OHLC 和 averagePrice 使用同一复权 ratio。
    - turnoverRate 使用 `turnover_rate_f` 优先；缺失时使用 `turnover_rate`；均缺失返回结构化缺失原因。
    - reader 不负责筹码算法。
  </behavior>
  <action>新增 `readAdjustedChipModelBarsForStock()` 或等价函数。输入 generationId、tsCode、startTradeDate、endTradeDate，输出排序后的模型 bars 或抛出/返回 `missing_adjustment_factor`、`missing_turnover_rate`、`missing_daily_quote`。成交均价计算公式使用 Tushare 单位：`amount * 1000 / (vol * 100)`，再按复权 ratio 调整；无法计算时 fallback 到 `(high + low + close) / 3`。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/market-data-reader.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 测试证明 OHLC 和 averagePrice 使用同一复权 ratio。
    - 缺失换手率返回/抛出 `missing_turnover_rate`，不继续生成模型 bar。
    - 缺失 adj_factor 返回/抛出现有 `missing_adjustment_factor`。
    - 现有筛选读取 `readAdjustedMarketData()` 行为不变。
  </acceptance_criteria>
  <done>后续算法可以依赖一个明确、复权一致、可测试的数据入口。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/refresh/fetch-refresh-data.test.ts tests/refresh/market-data-store.test.ts tests/refresh/market-data-reader.test.ts`
- `D:\NodeJS\npm.cmd run typecheck`
- `D:\NodeJS\npm.cmd run lint -- src/lib/tushare src/lib/refresh tests/refresh`
</verification>

<success_criteria>
- 当前 market generation 可以缓存成交额和换手率。
- 模型输入 reader 能提供复权一致的 OHLC、成交均价、换手率。
- 缺失换手率、缺失日线、缺失复权因子都有结构化错误。
- 没有使用成交量或固定常数代替换手率。
</success_criteria>

<output>
完成后创建 `.planning/phases/12-decay-based-chip-distribution-model/12-01-SUMMARY.md`。
</output>
