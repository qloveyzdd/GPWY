---
phase: 12-decay-based-chip-distribution-model
plan: "12-03"
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - src/lib/chip/chip-types.ts
  - src/lib/chip/chip-model-store.ts
  - src/lib/chip/chip-model-runner.ts
  - src/lib/chip/chip-runner.ts
  - src/lib/refresh/refresh-runner.ts
  - tests/chip/chip-model-store.test.ts
  - tests/chip/chip-model-runner.test.ts
  - tests/refresh/refresh-runner.test.ts
  - tests/smoke/seed-smoke-db.ts
  - tests/fixtures/chip-model/002565-20260626-20260629.json
autonomous: true
requirements: [DATA-10, DATA-11, DATA-12, CMOD-01, CMOD-03, CMOD-04, CMOD-05, VAL-01, VAL-02]
user_setup: []
must_haves:
  truths:
    - "D-12-10/D-12-18: 计算分布缓存 key 至少包含 tsCode、targetTradeDate、seedTradeDate、decayCoefficient、modelVersion。"
    - "D-12-12/D-12-13/D-12-14: 只对当前筛选结果后台计算，不做全市场回填，也不把详情点击作为默认首次计算路径。"
    - "D-12-17: 官方 cyq_chips、seed snapshot 和 calculated distribution 必须分开存储或用明确字段隔离，计算结果不能覆盖官方表。"
    - "D-12-19/D-12-20: 缺 seed、缺交易数据、缺换手率、缺复权因子都要记录结构化不可用；至少保留一组可复现对比 fixture。"
  artifacts:
    - path: "src/lib/chip/chip-model-store.ts"
      provides: "calculated chip distribution tables, seed snapshots, statuses, and work planning"
    - path: "src/lib/chip/chip-model-runner.ts"
      provides: "background calculation for selected stocks, two target dates, and all fixed coefficients"
    - path: "tests/fixtures/chip-model/002565-20260626-20260629.json"
      provides: "reproducible comparison fixture for coefficient effects"
  key_links:
    - from: "src/lib/chip/chip-model-runner.ts"
      to: "src/lib/chip/chip-model.ts"
      via: "executes pure model and persists results"
    - from: "src/lib/refresh/refresh-runner.ts"
      to: "src/lib/chip/chip-model-runner.ts"
      via: "starts calculated distribution stage after screening and official chip enrichment"
---

<objective>
把纯算法接入后台处理链路：定位 60 个交易日前种子分布，保存 seed snapshot，按 latest/previous 两个目标日和全部固定衰减系数生成可复用的计算分布缓存。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/12-decay-based-chip-distribution-model/12-CONTEXT.md
@.planning/phases/12-decay-based-chip-distribution-model/12-RESEARCH.md
@.planning/phases/12-decay-based-chip-distribution-model/12-PATTERNS.md
@src/lib/chip/chip-store.ts
@src/lib/chip/chip-runner.ts
@src/lib/chip/chip-model.ts
@src/lib/refresh/market-data-reader.ts
@src/lib/refresh/refresh-runner.ts
@src/lib/screening/screening-store.ts
@tests/chip/chip-runner.test.ts
@tests/refresh/refresh-runner.test.ts

<interfaces>
- 新增 calculated run/status/level 读写函数，命名应与官方 distribution store 明确区分。
- 新增 seed snapshot 表或等价结构，保存复权到目标口径后的 seed levels。
- runner 输出 progress：totalTargets、completedTargets、succeeded、blocked、failed、missing、skippedComplete。
- runner 必须可以在没有 token、缺 seed、缺 daily_basic 时记录 blocked/missing，不抛出导致刷新主流程失败。
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-12-05: 计算结果覆盖官方 `cyq_chips`，后续无法区分来源 | high | 独立 calculated 表和 seed snapshot 表，测试断言官方表不变 |
| T-12-06: 7 个系数 × 双日目标造成重复计算 | medium | work planner 按 cache key 跳过完整结果 |
| T-12-07: 某一天缺数据导致整只股票无图 | medium | status 按 target date + coefficient 隔离 |
| T-12-08: 后台计算阻塞筛选结果发布 | high | 接入现有后处理阶段，结果表先可用，计算状态后台推进 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立计算分布缓存和 seed snapshot store</name>
  <files>src/lib/chip/chip-model-store.ts, src/lib/chip/chip-types.ts, tests/chip/chip-model-store.test.ts, tests/smoke/seed-smoke-db.ts</files>
  <read_first>src/lib/chip/chip-store.ts, tests/chip/chip-runner.test.ts, tests/smoke/seed-smoke-db.ts</read_first>
  <behavior>
    - 计算 levels 不写入 `chip_distribution_levels`。
    - seed snapshot 与 targetTradeDate、seedTradeDate、modelVersion 绑定。
    - status 记录 targetKind、targetTradeDate、seedTradeDate、decayCoefficient、modelVersion。
    - cache 完整时可跳过重复计算。
  </behavior>
  <action>新增 `chip-model-store.ts`，包含 ensureSchema、replace/read calculated distribution、replace/read seed snapshot、write/read run/status、plan work。SQLite key 要覆盖 ts_code、target_trade_date、seed_trade_date、decay_coefficient、model_version。测试覆盖写入、更新、读取、跳过完整缓存和官方表隔离。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-model-store.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 同一股票/目标日/种子日/系数/版本的结果可复用。
    - 不同系数结果互不覆盖。
    - seed snapshot 可按 targetTradeDate 读取。
    - 官方 `chip_distribution_levels` 写入测试不受计算表影响。
  </acceptance_criteria>
  <done>计算分布具备独立持久化和缓存判断能力。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 定位种子日并获取官方种子分布</name>
  <files>src/lib/chip/chip-model-runner.ts, src/lib/chip/chip-runner.ts, tests/chip/chip-model-runner.test.ts</files>
  <read_first>src/lib/chip/chip-runner.ts, src/lib/refresh/market-data-reader.ts, src/lib/screening/screening-store.ts, tests/chip/chip-runner.test.ts</read_first>
  <behavior>
    - 对每个 target date 定位向前 60 个有效交易日前的 seedTradeDate。
    - seed 分布优先读取已有官方 `cyq_chips` 缓存；缺失时通过 provider 获取并写入官方缓存。
    - seed 分布转换到目标复权口径后保存为 seed snapshot。
    - provider 错误分类复用现有脱敏分类，不暴露 token 或原始 payload。
  </behavior>
  <action>实现 seed resolver。它从当前 screening run 的 source market generation 读取足够交易日；如当前 generation 只有 60 日不足以定位 60 日前 seed，需要在计划实现中扩展 generation target date count 或为模型补取 seed 区间数据，不能用现有 60 日窗口伪装 seed。对缺 seed date、缺 official cyq_chips、缺 factor 记录结构化状态。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-model-runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - target=`20260629` 时能选择第 60 个有效交易日前的 seedTradeDate。
    - seed official rows 已缓存时不重复请求 provider。
    - seed official rows 缺失且 provider 返回 empty data 时记录 `missing_seed_distribution` 或 blocked 状态。
    - seed snapshot 价格已按目标日复权 ratio 调整。
  </acceptance_criteria>
  <done>runner 能拿到可复现的模型初始状态。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 后台计算 latest/previous 和全部固定系数</name>
  <files>src/lib/chip/chip-model-runner.ts, src/lib/refresh/refresh-runner.ts, tests/chip/chip-model-runner.test.ts, tests/refresh/refresh-runner.test.ts, tests/fixtures/chip-model/002565-20260626-20260629.json</files>
  <read_first>src/lib/refresh/refresh-runner.ts, src/lib/chip/chip-model.ts, src/lib/chip/chip-model-store.ts, tests/refresh/refresh-runner.test.ts</read_first>
  <behavior>
    - 只对当前筛选结果股票计算。
    - latest 和 previous 独立 status。
    - 7 个固定系数全部预计算，UI 切换只读缓存。
    - 结果表发布不等待计算分布完成。
  </behavior>
  <action>实现 `runCalculatedChipDistributionIntegrationFromLatestScreening()`。它生成 target × coefficient work items，跳过完整缓存，读取模型 bars，调用纯算法，写入 seed snapshot、calculated levels 和 statuses。接入 `refresh-runner.ts` 的后处理阶段，位置应在筛选结果可用之后；如果官方 chip enrichment 失败，计算阶段仍可基于 seed 获取路径独立记录原因。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-model-runner.test.ts tests/refresh/refresh-runner.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 一个入选股票、两个目标日、7 个系数会生成 14 个 status 或明确跳过缓存。
    - latest 缺数据不影响 previous 成功。
    - 缓存完整时第二次运行不重复写入 levels。
    - fixture 包含至少一个股票/两个日期/多个系数的输出快照，用于后续 UI 和算法比较。
  </acceptance_criteria>
  <done>刷新后处理可以生成可复用的计算分布缓存。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-model-store.test.ts tests/chip/chip-model-runner.test.ts tests/refresh/refresh-runner.test.ts`
- `D:\NodeJS\npm.cmd run typecheck`
- `D:\NodeJS\npm.cmd run lint -- src/lib/chip src/lib/refresh tests/chip tests/refresh`
</verification>

<success_criteria>
- 60 日前 seed 日能被定位并缓存。
- 官方原始分布、seed snapshot、计算分布互相隔离。
- 计算结果按股票/目标日/种子日/系数/模型版本复用。
- 后台计算覆盖 latest/previous 和全部固定系数。
- 缺失 seed、交易数据、换手率、复权因子时有结构化状态。
</success_criteria>

<output>
完成后创建 `.planning/phases/12-decay-based-chip-distribution-model/12-03-SUMMARY.md`。
</output>
