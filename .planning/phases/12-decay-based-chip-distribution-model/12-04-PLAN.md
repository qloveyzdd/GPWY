---
phase: 12-decay-based-chip-distribution-model
plan: "12-04"
type: execute
wave: 3
depends_on: ["12-03"]
files_modified:
  - src/lib/results/chart-types.ts
  - src/lib/results/chart-data.ts
  - src/components/charts/stock-kline-chart.tsx
  - tests/results/chart-data.test.ts
  - tests/ui/stock-kline-chart.test.tsx
  - tests/smoke/seed-smoke-db.ts
  - tests/smoke/app-smoke.spec.ts
autonomous: true
requirements: [CMOD-03, CMOD-04, CMOD-05, UI-08, UI-09, UI-10, VAL-02]
user_setup: []
must_haves:
  truths:
    - "D-12-06/D-12-11: UI 必须明确标注计算分布，不得暗示它等同官方 cyq_chips 或交易软件真实筹码。"
    - "D-12-07/D-12-08/D-12-09/D-12-10: 系数选择只允许固定集合，默认 0.5，切换后读取对应缓存。"
    - "D-12-14/D-12-15/D-12-16: 详情页不负责首次计算；latest/previous 任一不可用时，另一日和官方分布仍可显示。"
    - "UI-08/UI-09/UI-10/VAL-02: 图表展示要支持系数切换、口径说明和可复现对比数据。"
  artifacts:
    - path: "src/lib/results/chart-types.ts"
      provides: "official and calculated chip distribution DTO for the stock detail chart"
    - path: "src/lib/results/chart-data.ts"
      provides: "server aggregation of calculated distribution cache and unavailable reasons"
    - path: "src/components/charts/stock-kline-chart.tsx"
      provides: "coefficient selector and calculated distribution display"
  key_links:
    - from: "src/lib/results/chart-data.ts"
      to: "src/lib/chip/chip-model-store.ts"
      via: "reads cached calculated distributions by selected coefficients"
    - from: "src/components/charts/stock-kline-chart.tsx"
      to: "src/lib/results/chart-types.ts"
      via: "renders cached computed distribution panels and source metadata"
---

<objective>
把计算分布接入股票详情页：服务端 DTO 返回官方分布和计算分布，前端默认展示系数 0.5 的计算分布，并允许在固定衰减系数集合中切换；当计算分布不可用时保留官方图并解释原因。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/12-decay-based-chip-distribution-model/12-CONTEXT.md
@.planning/phases/12-decay-based-chip-distribution-model/12-RESEARCH.md
@.planning/phases/12-decay-based-chip-distribution-model/12-PATTERNS.md
@.planning/milestones/v2.0-phases/11-distribution-comparison-experience/11-CONTEXT.md
@src/lib/results/chart-types.ts
@src/lib/results/chart-data.ts
@src/lib/chip/chip-model-store.ts
@src/lib/chip/chip-types.ts
@src/components/charts/stock-kline-chart.tsx
@tests/results/chart-data.test.ts
@tests/ui/stock-kline-chart.test.tsx

<interfaces>
- Chart DTO 保留现有官方 `chipDistributions`，新增 calculated 分布数据或显式 source 字段，避免破坏 Phase 11 双日官方展示。
- calculated panel 至少包含 targetKind、status、targetTradeDate、seedTradeDate、decayCoefficient、modelVersion、levels、maxLevel、errorSummary。
- UI 使用本地 state 切换系数，不发起计算请求。
- 计算分布不可用时展示对应原因，并继续显示官方分布。
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-12-07: 用户误以为计算分布是官方或通达信/同花顺原始数据 | high | 卡片标题和元数据明确写“计算分布 / 模型输出” |
| T-12-08: coefficient selector 触发运行时计算导致详情页卡顿 | medium | DTO 只读取缓存，缺失则显示 unavailable |
| T-12-09: 计算分布失败时覆盖官方图，用户失去参考 | medium | UI 分离 official fallback 和 calculated unavailable |
| T-12-10: latest/previous 尺度不一致导致视觉误导 | medium | 计算分布保持 shared scale，官方 fallback 使用已有 shared scale |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 Chart DTO 和服务端聚合</name>
  <files>src/lib/results/chart-types.ts, src/lib/results/chart-data.ts, tests/results/chart-data.test.ts</files>
  <read_first>src/lib/results/chart-types.ts, src/lib/results/chart-data.ts, src/lib/chip/chip-model-store.ts, tests/results/chart-data.test.ts</read_first>
  <behavior>
    - 官方分布 DTO 继续可用。
    - 计算分布 DTO 按 coefficient 分组。
    - 默认 0.5 的数据在 DTO 中可直接读取。
    - 不可用原因按 targetKind + coefficient 隔离。
  </behavior>
  <action>在 `chart-types.ts` 增加 calculated distribution 类型；在 `chart-data.ts` 读取最新 calculated run/status/levels，为 7 个系数构造缓存视图。生成计算分布 shared scale；若某系数没有任何成功 levels，scale 为安全空值且 status 保留。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/results/chart-data.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - chart-data 测试覆盖默认 0.5 succeeded。
    - chart-data 测试覆盖 0.3/1.0 等不同系数互不污染。
    - latest failed + previous succeeded 时 previous levels 正常返回。
    - DTO 不泄露 token、headers、本地绝对路径或 provider 原始 payload。
  </acceptance_criteria>
  <done>前端可以从 ChartSnapshot 读取计算分布缓存。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现系数切换和计算分布标注</name>
  <files>src/components/charts/stock-kline-chart.tsx, tests/ui/stock-kline-chart.test.tsx</files>
  <read_first>src/components/charts/stock-kline-chart.tsx, src/lib/results/chart-types.ts, tests/ui/stock-kline-chart.test.tsx</read_first>
  <behavior>
    - 默认选中 0.5。
    - 只展示固定系数集合。
    - 每张计算分布图显示目标日、种子日、系数、模型版本。
    - 标题或说明必须包含“计算分布”。
  </behavior>
  <action>在 `StockKlineChart` 的分布区域增加 coefficient selector。selector 切换只改变本地展示数据，不调用 API。计算分布可用时渲染 latest/previous 双图；不可用时显示原因和官方分布 fallback。保持 Phase 11 的行内详情布局，不引入弹窗或新导航。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/stock-kline-chart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 默认渲染 0.5。
    - 点击/选择 1.0 后柱状图数据切换到 1.0。
    - 页面文案包含“计算分布”、目标日、种子日和衰减系数。
    - selector 不包含自由输入项。
  </acceptance_criteria>
  <done>用户能在详情页比较不同衰减系数下的计算分布。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 完成 fallback、fixture 和 smoke 验证</name>
  <files>tests/smoke/seed-smoke-db.ts, tests/smoke/app-smoke.spec.ts, tests/results/chart-data.test.ts, tests/ui/stock-kline-chart.test.tsx</files>
  <read_first>tests/smoke/seed-smoke-db.ts, tests/smoke/app-smoke.spec.ts, tests/fixtures/chip-model/002565-20260626-20260629.json</read_first>
  <behavior>
    - smoke seed 包含至少一个 computed succeeded 和一个 unavailable 场景。
    - fixture 能复现不同系数导致的分布差异。
    - official fallback 不被 calculated unavailable 覆盖。
    - 最终 grep 不应出现把计算分布标成官方分布的文案。
  </behavior>
  <action>更新 smoke DB seed，加入 calculated distribution tables 和示例数据。扩展 Playwright smoke：展开结果行，确认系数 selector、计算分布标签、fallback 和双日展示存在。用 fixture 在 unit test 中断言 0.5 与 1.5 的 maxLevel 或高位筹码留存存在可解释差异。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/results/chart-data.test.ts tests/ui/stock-kline-chart.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run smoke -- tests/smoke/app-smoke.spec.ts --reporter=line</automated>
    <automated>D:\NodeJS\npm.cmd run verify</automated>
  </verify>
  <acceptance_criteria>
    - smoke 能看到计算分布和官方 fallback。
    - fixture 驱动的测试证明不同系数会改变分布形态。
    - full verify 通过。
  </acceptance_criteria>
  <done>Phase 12 UI 路径和可复现对比数据完成闭环。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/results/chart-data.test.ts tests/ui/stock-kline-chart.test.tsx`
- `D:\NodeJS\npm.cmd run smoke -- tests/smoke/app-smoke.spec.ts --reporter=line`
- `D:\NodeJS\npm.cmd run verify`
</verification>

<success_criteria>
- 股票详情页可以切换固定衰减系数。
- 默认展示 0.5 的计算分布。
- 计算分布明确显示目标日、种子日、系数、模型版本和“计算分布”口径。
- 计算分布不可用时，官方分布或另一日可用图仍显示。
- fixture 和 smoke 证明不同系数下分布形态可复现比较。
</success_criteria>

<output>
完成后创建 `.planning/phases/12-decay-based-chip-distribution-model/12-04-SUMMARY.md`。
</output>
