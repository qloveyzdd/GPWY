---
phase: 10-dual-day-chip-distribution
plan: "10-04"
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - src/lib/refresh/refresh-runner.ts
  - src/lib/refresh/refresh-types.ts
  - src/lib/chip/chip-runner.ts
  - tests/refresh/refresh-runner.test.ts
  - tests/ui/status-workspace.test.tsx
  - tests/smoke/seed-smoke-db.ts
autonomous: true
requirements: [CHIP-05, CHIP-06, CHIP-07, CHIP-08, CHIP-09, CHIP-10]
user_setup: []
must_haves:
  truths:
    - "Phase 10 end-to-end: chip_background 阶段必须运行新的双日完整分布 runner，而不是旧单日前三峰 runner。"
    - "D-10-01/D-10-02: chip stage progress 和失败统计按目标日期聚合，部分日期成功仍可用。"
    - "D-10-05/D-10-06/D-10-07/D-10-08: 刷新集成必须体现缓存复用、failed 自动重试、blocked 不自动重试。"
    - "D-10-09/D-10-10: 页面兼容字段继续可用，smoke seed 使用完整分布源数据。"
    - "CHIP-05/CHIP-06/CHIP-07/CHIP-08/CHIP-09/CHIP-10: 所有 Phase 10 requirements 至少被一个端到端测试路径覆盖。"
  artifacts:
    - path: "src/lib/refresh/refresh-runner.ts"
      provides: "Manual refresh integration with dual-day chip distribution background operation"
      exports: ["startManualRefresh", "readRefreshStatus"]
    - path: "tests/refresh/refresh-runner.test.ts"
      provides: "Refresh-to-chip-background integration tests"
    - path: "tests/smoke/seed-smoke-db.ts"
      provides: "Smoke database seed using new complete distribution schema"
  key_links:
    - from: "src/lib/refresh/refresh-runner.ts"
      to: "src/lib/chip/chip-runner.ts"
      via: "chip_background invokes `runChipDistributionIntegrationFromLatestScreening`"
    - from: "src/components/status/status-workspace.tsx"
      to: "src/lib/refresh/refresh-types.ts"
      via: "status UI consumes unchanged chip stage snapshot fields"
---

<objective>
把新的双日完整筹码分布 runner 接入 Phase 9 的后台筹码阶段，并完成状态 UI、smoke seed 和全阶段验证收口。

Purpose: Phase 10 的后端能力必须在真实手动刷新路径中生效，而不是只存在于 chip 模块单测；同时保证现有 UI 与 smoke 测试仍能读取兼容字段。
Output: refresh runner 集成、chip stage 日期级进度、status/smoke 测试更新、全阶段验证通过。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-dual-day-chip-distribution/10-CONTEXT.md
@.planning/phases/10-dual-day-chip-distribution/10-RESEARCH.md
@.planning/phases/10-dual-day-chip-distribution/10-VALIDATION.md
@.planning/phases/10-dual-day-chip-distribution/10-PATTERNS.md
@.planning/phases/10-dual-day-chip-distribution/10-02-SUMMARY.md
@.planning/phases/10-dual-day-chip-distribution/10-03-SUMMARY.md
@src/lib/refresh/refresh-runner.ts
@src/lib/refresh/refresh-types.ts
@src/lib/chip/chip-runner.ts
@src/components/status/status-workspace.tsx
@tests/refresh/refresh-runner.test.ts
@tests/ui/status-workspace.test.tsx
@tests/smoke/seed-smoke-db.ts

<interfaces>
- `ChipDistributionWorkflowRunner = (options: { now?: Date; onProgress?: (progress: ChipDistributionProgress) => void }) => Promise<ChipDistributionRunRecord>`。
- `startChipBackground` uses distribution runner and maps progress to `RefreshStageKey="chip"`。
- `readRefreshStatus().chipVersion` changes when latest distribution run changes.
- Existing UI stage key remains `chip` with label `筹码处理`。
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-10-06: refresh status 泄露 provider 原始错误或本地路径 | high | `sanitizeErrorSummary` continues to redact token/path/header-like content; tests cover chip stage error summary. |
| T-10-08: 后台筹码异常回写 refresh job，破坏已发布筛选结果 | medium | chip_background operation completion/failure remains independent from manual_refresh job success. |
| T-10-09: smoke seed 继续写旧 chip_peak 表导致真实页面路径未覆盖 | low | smoke seed 改为写完整分布表，再通过兼容投影显示旧字段。 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 将 refresh runner 接入新的双日分布 runner</name>
  <files>src/lib/refresh/refresh-runner.ts, src/lib/refresh/refresh-types.ts, src/lib/chip/chip-runner.ts, tests/refresh/refresh-runner.test.ts</files>
  <read_first>src/lib/refresh/refresh-runner.ts, src/lib/refresh/refresh-types.ts, src/lib/chip/chip-runner.ts, tests/refresh/refresh-runner.test.ts, .planning/phases/10-dual-day-chip-distribution/10-02-SUMMARY.md</read_first>
  <behavior>
    - manual refresh screening 成功后自动启动新的 distribution runner。
    - chip progress total/completed/failed 按目标日期统计，而不是只按股票统计。
    - chip run partial 不影响 latestSuccessfulJob。
    - `chipVersion` 基于 latest distribution run id/createdAt/status。
  </behavior>
  <action>在 `refresh-runner.ts` 将 `ChipPeakWorkflowRunner` 替换或兼容扩展为 `ChipDistributionWorkflowRunner`，默认 runner 使用 `runChipDistributionIntegrationFromLatestScreening`。`startChipBackground` 的 onProgress 映射：total=`progress.totalTargets`，completed=`progress.completedTargets`，failed=`progress.failed + progress.blocked + progress.missing`。`chipStageStatus` 使用 distribution run status；run status partial 映射 stage partial，failed 映射 failed，succeeded 映射 succeeded。`createChipVersion` 改为读取 latest distribution run；如保留函数名，不再读取旧 chip peak run。测试替换 `chipRunFixture` 为 distribution run fixture，并保留“chip 后台运行时拒绝新 manual refresh”。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/refresh-runner.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `startManualRefresh({ waitForCompletion: true })` 完成后 chip runner 已被调用一次
    - chip runner deferred 时 `readRefreshStatus().activeOperation.kind === "chip_background"`
    - chip partial run 完成后 stage `chip.status === "partial"` 且 failed 数包含 blocked/missing/failed
    - chip runner throw 后 latest successful refresh job 仍存在
  </acceptance_criteria>
  <done>真实刷新路径使用新的双日完整分布后台阶段。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新状态 UI 测试和 smoke seed 到完整分布源</name>
  <files>tests/ui/status-workspace.test.tsx, tests/smoke/seed-smoke-db.ts, tests/results/results-snapshot.test.ts</files>
  <read_first>tests/ui/status-workspace.test.tsx, tests/smoke/seed-smoke-db.ts, src/components/status/status-workspace.tsx, src/lib/chip/chip-store.ts, tests/smoke/app-smoke.spec.ts</read_first>
  <behavior>
    - status UI 仍显示 `筹码处理` 阶段、partial/failed 数量和 chipVersion marker。
    - smoke seed 写入新完整分布表后，页面仍能看到兼容筹码峰展示。
    - 旧 chip_peak seed 不再是 smoke 的唯一筹码来源。
  </behavior>
  <action>更新 `tests/ui/status-workspace.test.tsx` 的 fixture，使 chipVersion 可以来自 distribution run，阶段字段仍使用 `chip`。更新 `tests/smoke/seed-smoke-db.ts`：创建并填充 Phase 10 新 distribution schema，写入至少一个 latest target succeeded 的完整分布和一个 previous target succeeded/blocked 状态；确保现有 smoke 页面仍能通过兼容投影显示旧筹码峰。若旧 `chip_peak_*` 表仍需为迁移兼容保留，可保留建表但不要让 smoke 依赖它作为唯一来源。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/status-workspace.test.tsx tests/results/results-snapshot.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run smoke -- tests/smoke/app-smoke.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - status workspace 测试仍能检测 chipVersion 变化后触发结果刷新
    - smoke seed 文件包含 `chip_distribution_runs` 或执行新 store 写入路径
    - smoke 测试不依赖旧 `chip_peak_levels` 才能展示筹码峰
    - 页面状态文案仍使用 `筹码处理`
  </acceptance_criteria>
  <done>状态 UI 和 smoke 数据覆盖新完整分布路径。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 执行 Phase 10 全量验证和兼容清理</name>
  <files>src/lib/chip/chip-store.ts, src/lib/chip/chip-runner.ts, src/lib/results/results-snapshot.ts, src/lib/refresh/refresh-runner.ts, tests/chip/chip-distribution-store.test.ts, tests/chip/chip-runner.test.ts, tests/results/results-snapshot.test.ts, tests/refresh/refresh-runner.test.ts</files>
  <read_first>.planning/phases/10-dual-day-chip-distribution/10-VALIDATION.md, package.json, src/lib/chip/chip-store.ts, src/lib/chip/chip-runner.ts, src/lib/results/results-snapshot.ts, src/lib/refresh/refresh-runner.ts</read_first>
  <behavior>
    - 所有 Phase 10 requirements 有自动化测试覆盖。
    - 旧 chip peak API 若保留，必须明确作为兼容层，不再作为结果事实源。
    - typecheck、lint、affected tests 和 production build 通过。
  </behavior>
  <action>按 `10-VALIDATION.md` 执行最终验证。清理 Phase 10 中遗留的直接旧 store 读取：`results-snapshot.ts` 不应再调用 `readLatestChipPeakRun` 或 `readChipPeakResultsForRun` 作为主要路径；`refresh-runner.ts` 不应默认调用 `runChipPeakIntegrationFromLatestScreening` 作为后台 runner。保留旧导出时用注释或命名说明 legacy compatibility，避免误用。运行 affected tests、`npm run verify`，如 Playwright browser 缺失，先报告并只在环境允许时运行 `npx playwright install chromium` 后重试 smoke。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-distribution-store.test.ts tests/chip/chip-runner.test.ts tests/results/results-snapshot.test.ts tests/results/chart-data.test.ts tests/refresh/refresh-runner.test.ts tests/ui/status-workspace.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run verify</automated>
    <automated>D:\NodeJS\npm.cmd run smoke -- tests/smoke/app-smoke.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify` exits 0
    - affected Phase 10 Vitest command exits 0
    - smoke test exits 0 or environment blocker is documented in SUMMARY with exact missing dependency
    - `rg "runChipPeakIntegrationFromLatestScreening|readLatestChipPeakRun|readChipPeakResultsForRun" src/lib/results src/lib/refresh` does not show those APIs as the primary Phase 10 path
  </acceptance_criteria>
  <done>Phase 10 后端完整分布能力完成并通过全量验证。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-distribution-store.test.ts tests/chip/chip-runner.test.ts tests/results/results-snapshot.test.ts tests/results/chart-data.test.ts tests/refresh/refresh-runner.test.ts tests/ui/status-workspace.test.tsx`
- `D:\NodeJS\npm.cmd run verify`
- `D:\NodeJS\npm.cmd run smoke -- tests/smoke/app-smoke.spec.ts --reporter=line`
</verification>

<success_criteria>
- refresh runner 使用双日完整分布 runner。
- chip stage 进度和版本 marker 反映 distribution run。
- status UI 和 smoke seed 覆盖新完整分布路径。
- Phase 10 所有 requirements 和 CONTEXT 决策均被自动化验证覆盖。
</success_criteria>

<output>
完成后创建 `.planning/phases/10-dual-day-chip-distribution/10-04-SUMMARY.md`。
</output>
