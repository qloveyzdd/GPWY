---
phase: 10-dual-day-chip-distribution
plan: "10-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/chip/chip-types.ts
  - src/lib/chip/chip-peak.ts
  - src/lib/chip/chip-store.ts
  - tests/chip/chip-distribution-store.test.ts
  - tests/chip/chip-peak.test.ts
autonomous: true
requirements: [CHIP-07, CHIP-08, CHIP-09, CHIP-10]
user_setup: []
must_haves:
  truths:
    - "D-10-01/D-10-02/D-10-03: 筹码状态以股票+交易日为单位独立保存，某一日期失败不影响另一日期成功数据可用。"
    - "D-10-05/D-10-06/D-10-07/D-10-08: 完整缓存定义为 succeeded 且至少 1 个价格档位；succeeded 跳过、failed 重试、blocked 不自动重试。"
    - "D-10-13/D-10-15/D-10-16: 每个目标日期独立落库，单事务完整替换旧价格档位，空分布为 blocked/empty_data。"
    - "CHIP-07/CHIP-08/CHIP-09/CHIP-10: 完整价格档位和状态是源数据，不再用前三峰作为源存储。"
  artifacts:
    - path: "src/lib/chip/chip-store.ts"
      provides: "股票-交易日完整筹码分布缓存、状态读写和 work planning API"
      exports: ["writeChipDistributionRun", "replaceChipDistribution", "planChipDistributionWork", "readLatestChipDistributionRun"]
    - path: "src/lib/chip/chip-types.ts"
      provides: "完整筹码分布、日期级状态、目标日期和兼容峰 DTO 类型"
      contains: "ChipDistributionStatus"
    - path: "tests/chip/chip-distribution-store.test.ts"
      provides: "完整替换、状态复用、failed/blocked/missing 差集规划回归测试"
  key_links:
    - from: "src/lib/chip/chip-store.ts"
      to: "src/lib/chip/chip-types.ts"
      via: "store functions return typed stock-date distribution cache DTOs"
    - from: "tests/chip/chip-distribution-store.test.ts"
      to: "src/lib/chip/chip-store.ts"
      via: "tests prove store work planning and transactional replacement semantics"
---

<objective>
建立 Phase 10 的完整筹码分布源数据模型：按股票和交易日保存全部价格档位、日期级状态、完整性判定和可恢复 work planning。

Purpose: 后续 runner、结果快照兼容层和刷新阶段状态都必须依赖同一个可复用缓存模型；先固定事实源，避免继续把旧前三峰 run 快照当缓存。
Output: 完整分布 TypeScript 类型、SQLite additive schema、事务替换 API、work planning API 和 store 单元测试。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-dual-day-chip-distribution/10-CONTEXT.md
@.planning/phases/10-dual-day-chip-distribution/10-RESEARCH.md
@.planning/phases/10-dual-day-chip-distribution/10-VALIDATION.md
@.planning/phases/10-dual-day-chip-distribution/10-PATTERNS.md
@src/lib/chip/chip-types.ts
@src/lib/chip/chip-peak.ts
@src/lib/chip/chip-store.ts
@tests/chip/chip-store.test.ts
@tests/chip/chip-peak.test.ts

<interfaces>
- `ChipDistributionTargetKind = "latest" | "previous"`。
- `ChipDistributionStatus = "succeeded" | "blocked" | "failed" | "missing"`。
- `ChipDistributionLevel` 至少包含 tsCode、tradeDate、price、percent。
- `ChipDistributionTarget` 至少包含 screeningRunId、tsCode、targetKind、tradeDate。
- `planChipDistributionWork(targets)` 必须跳过完整 succeeded，返回 failed 和未写入目标，不返回 blocked 或 missing。
- `replaceChipDistribution({ tsCode, tradeDate, levels, now })` 必须在单事务内删除该股票-日期旧档位并插入新档位。
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-10-01: provider 错误摘要泄露 token、headers、本地路径或原始响应 | high | Store 层只接受已分类的 `errorCategory/errorSummary`，测试断言公开 DTO 不包含 `TUSHARE_TOKEN`、Windows 路径或 headers 字样。 |
| T-10-02: 非事务写入导致旧价格档位残留，产生错误筹码分布 | medium | `replaceChipDistribution` 使用 `begin`/`commit`/`rollback` 删除再插入；测试先写 3 档再替换 1 档并断言旧档位不存在。 |
| T-10-03: 把 `blocked` 纳入自动重试导致重复打确定性失败接口 | medium | `planChipDistributionWork` 明确跳过 blocked/missing，只返回 failed 和未写入目标。 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展完整分布类型和 cyq_chips 映射能力</name>
  <files>src/lib/chip/chip-types.ts, src/lib/chip/chip-peak.ts, tests/chip/chip-peak.test.ts</files>
  <read_first>src/lib/chip/chip-types.ts, src/lib/chip/chip-peak.ts, tests/chip/chip-peak.test.ts, .planning/phases/10-dual-day-chip-distribution/10-CONTEXT.md</read_first>
  <behavior>
    - `mapCyqChipsTable` 继续返回完整分布行，且不丢弃同一日期的任何价格档位。
    - 兼容前三峰提取仍按 `percent desc`、`price asc` 排序。
    - 新类型表达 targetKind、日期级状态、完整分布 run 统计和错误字段。
  </behavior>
  <action>在 `chip-types.ts` 增加 `ChipDistributionTargetKind`、`ChipDistributionStatus`、`ChipDistributionLevel`、`ChipDistributionTarget`、`ChipDistributionStatusRecord`、`ChipDistributionRunRecord`、`ChipDistributionWorkItem` 类型。保留现有 `ChipPeakLevel` 和 `ChipPeakResultRecord` 兼容类型，避免现有结果表格和 K 线编译失败。在 `chip-peak.ts` 保留 `mapCyqChipsTable`，新增 `deriveChipPeakLevelsFromDistribution(rows, limit = 3)`，该函数只接收单一交易日分布并返回旧 `ChipPeakLevel[]`；如果输入为空则抛出 `empty_chip_distribution`。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-peak.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `chip-types.ts` 导出 `ChipDistributionStatus`，其联合值包含 `succeeded`、`blocked`、`failed`、`missing`
    - `deriveChipPeakLevelsFromDistribution` 对同一日期多档位返回最多 3 个峰
    - 空分布测试仍抛出 `empty_chip_distribution`
    - 现有 `ChipPeakResultRecord` 引用不需要在本任务中全部迁移
  </acceptance_criteria>
  <done>完整分布和旧峰兼容派生拥有明确类型边界。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立完整筹码分布缓存 schema 和事务写入 API</name>
  <files>src/lib/chip/chip-store.ts, tests/chip/chip-distribution-store.test.ts</files>
  <read_first>src/lib/chip/chip-store.ts, tests/chip/chip-store.test.ts, .planning/phases/10-dual-day-chip-distribution/10-PATTERNS.md</read_first>
  <behavior>
    - 同一股票同一交易日写入新分布时，旧价格档位被完整替换。
    - 状态以股票+日期独立保存，latest failed 不影响 previous succeeded。
    - `succeeded` 且至少有 1 个 level 才被完整性检查视为 complete。
    - `blocked/empty_data` 可保存 errorCategory=`empty_data` 和脱敏 errorSummary。
  </behavior>
  <action>在 `chip-store.ts` 通过 additive migration 创建完整分布表。建议最少包含 `chip_distribution_runs`、`chip_distribution_statuses`、`chip_distribution_levels`。`chip_distribution_levels` 的缓存身份必须包含 `ts_code` 和 `trade_date`，不能只依赖 run id。导出 `replaceChipDistribution({ tsCode, tradeDate, levels, now? })`，在单事务内删除 `(ts_code, trade_date)` 旧 levels 并插入所有新 levels。导出 `upsertChipDistributionStatus(record, now?)` 保存 targetKind、tradeDate、status、errorCategory、errorSummary、source。导出 `readChipDistributionForDate(tsCode, tradeDate)`、`readChipDistributionStatusesForRun(runId)`、`writeChipDistributionRun(input)`、`readLatestChipDistributionRun()`。测试使用 `REFRESH_DB_PATH` 临时库，覆盖三档替换为一档、两个日期状态独立、空分布不能写成 succeeded。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-distribution-store.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `replaceChipDistribution` 替换后 `readChipDistributionForDate` 不返回旧 price
    - `chip_distribution_statuses` 能同时保存同一 tsCode 的 latest 和 previous 两条状态
    - `succeeded` 状态但无 levels 的记录不会被 `isChipDistributionComplete` 或同等 API 判定为完整
    - 公开读取 DTO 不包含 SQLite 文件路径、token、headers 或 provider 原始 payload 字段
  </acceptance_criteria>
  <done>数据库能可靠保存完整分布和日期级状态，并避免旧档位残留。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现筹码缓存 work planning</name>
  <files>src/lib/chip/chip-store.ts, tests/chip/chip-distribution-store.test.ts</files>
  <read_first>src/lib/chip/chip-store.ts, src/lib/refresh/market-data-store.ts, tests/refresh/market-data-store.test.ts, tests/chip/chip-distribution-store.test.ts</read_first>
  <behavior>
    - 完整 succeeded 目标不进入 work list。
    - failed 目标进入 work list，下一次刷新自动重试。
    - blocked 和 missing 目标不进入 work list。
    - 未见过的目标进入 work list。
  </behavior>
  <action>在 `chip-store.ts` 导出 `planChipDistributionWork(targets)`。输入为 `ChipDistributionTarget[]`，输出包含 requestable targets、skippedCompleteCount、blockedCount、missingCount、failedRetryCount。判定完整缓存时必须同时满足目标 date 状态为 `succeeded` 且 `readChipDistributionForDate(tsCode, tradeDate).levels.length > 0`。`failed` 和未写入状态返回为 work item；`blocked`、`missing` 不返回为 work item。测试覆盖四类状态组合，并断言同一 tsCode 的 latest complete 不会让 previous failed 被跳过。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-distribution-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - work planner 对 complete/latest 返回 skippedCompleteCount=1 且 work items 不含该目标
    - work planner 对 failed/previous 返回 failedRetryCount=1 且 work items 包含该目标
    - work planner 对 blocked/latest 不返回 work item
    - work planner 对 missing/previous 不返回 work item
  </acceptance_criteria>
  <done>后续 runner 可以只请求缺失或可恢复失败的股票-日期目标。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-distribution-store.test.ts tests/chip/chip-peak.test.ts`
- `D:\NodeJS\npm.cmd run typecheck`
- `D:\NodeJS\npm.cmd run lint -- src/lib/chip tests/chip`
</verification>

<success_criteria>
- 完整筹码分布以股票+交易日为缓存身份保存。
- 同一股票同一日期写入使用事务完整替换，不残留旧档位。
- 完整缓存、failed retry、blocked skip、missing skip 的 work planning 被测试覆盖。
- 旧前三峰仍可从完整分布派生，但不再作为源数据。
</success_criteria>

<output>
完成后创建 `.planning/phases/10-dual-day-chip-distribution/10-01-SUMMARY.md`。
</output>
