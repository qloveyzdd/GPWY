---
phase: 06-charts-and-deployment
plan: "06-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/refresh/refresh-runner.ts
  - src/lib/refresh/refresh-store.ts
  - src/lib/screening/screening-runner.ts
  - src/lib/results/chart-types.ts
  - src/lib/results/chart-data.ts
  - src/app/api/results/chart/[tsCode]/route.ts
  - src/components/status/status-workspace.tsx
  - tests/refresh/refresh-runner.test.ts
  - tests/results/chart-data.test.ts
  - tests/ui/status-workspace.test.tsx
autonomous: true
requirements: [CHRT-06]
must_haves:
  truths:
    - "D-06-01: 手动刷新必须串联缓存刷新、下降趋势筛选和筹码峰 enrichment。"
    - "D-06-02: 筹码峰失败不能阻塞筛选结果显示。"
    - "D-06-03: 成功手动刷新后不应继续停留在结果数据不可用，除非确实无匹配股票。"
    - "CHRT-06: 图表数据必须复用持久化筛选/筹码结果中的关键数值。"
  artifacts:
    - path: "src/lib/refresh/refresh-runner.ts"
      provides: "manual refresh full workflow orchestration"
    - path: "src/lib/results/chart-data.ts"
      provides: "selected-stock chart snapshot sourced from persisted result rows and matching cached bars"
---

## Phase Goal

**As a** user, **I want** one manual refresh to produce usable screening results and chart data, **so that** the table and chart reflect the same completed workflow.

<objective>
把手动刷新从“只写缓存”升级为“写缓存 -> 筛选 -> 筹码峰 enrichment”的完整流程，并提供与表格一致的单股图表数据读取层。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-charts-and-deployment/06-CONTEXT.md
@.planning/phases/06-charts-and-deployment/06-RESEARCH.md
@.planning/phases/05-results-table-experience/05-02-SUMMARY.md

<interfaces>
- Reuse `runDowntrendScreeningFromCache()` and `runChipPeakIntegrationFromLatestScreening()`.
- Extend cache readers so screening and chart helpers can read bars by the refresh job that produced a screening run.
- Keep `readLatestResultsSnapshot()` as the table source; do not make UI recompute screening values.
- Add an authenticated chart API for a selected `tsCode`; do not expose data for unauthenticated requests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add full-workflow refresh tests</name>
  <files>tests/refresh/refresh-runner.test.ts</files>
  <behavior>
    - A successful manual refresh writes cache data, creates a screening run, and attempts chip peak enrichment.
    - Screening failure marks the refresh workflow failed with a sanitized error summary.
    - Chip peak provider failure does not fail the refresh workflow and does not remove matched screening rows.
    - `waitForCompletion: true` returns after the full workflow, not after cache write only.
  </behavior>
  <action>Add focused tests with temporary SQLite fixtures and injected provider/chip behavior before changing orchestration.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/refresh-runner.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrate cache refresh, screening, and chip enrichment</name>
  <files>src/lib/refresh/refresh-runner.ts, src/lib/refresh/refresh-store.ts, src/lib/screening/screening-runner.ts</files>
  <behavior>
    - The active refresh job remains running while screening and chip enrichment are attempted.
    - Screening uses the current refresh job's cached stock basics and daily bars, not a stale latest-success lookup.
    - If screening succeeds, the refresh job can complete even when chip rows are blocked, failed, or missing.
    - No token, local path, provider payload, or stack trace is exposed in job errors.
  </behavior>
  <action>Introduce minimal job-scoped cache readers and wire the existing screening/chip runners into `finishRefreshJob()`.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/refresh-runner.test.ts tests/screening/screening-runner.test.ts tests/chip/chip-runner.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add chart data snapshot and selected-stock API</name>
  <files>src/lib/results/chart-types.ts, src/lib/results/chart-data.ts, src/app/api/results/chart/[tsCode]/route.ts, tests/results/chart-data.test.ts</files>
  <behavior>
    - Chart snapshot for a selected stock includes the persisted `ResultRow`, 60 cached OHLC bars, MA20/MA60 series, interval high overlay, 85% threshold overlay, and optional chip peak overlay.
    - The helper reads daily bars from the screening run's `sourceRefreshJobId`.
    - Unknown stock codes return `not_found`; no screening run returns `unavailable`.
    - Numeric overlay values match the table row values exactly.
    - The API requires the existing session gate and returns only sanitized chart data.
  </behavior>
  <action>Create chart data types/helper and an authenticated API route for client chart loading.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/results/chart-data.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Refresh server-rendered snapshots after workflow completion</name>
  <files>src/components/status/status-workspace.tsx, tests/ui/status-workspace.test.tsx</files>
  <behavior>
    - After a background refresh transitions from running to not running, the page refreshes server props once.
    - The results table can update from `unavailable` to `ready` or `empty` without a manual browser reload.
    - Polling failures keep the last known state visible and do not create refresh loops.
  </behavior>
  <action>Use the App Router client refresh pattern after polling observes workflow completion.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/status-workspace.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run verify</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Clicking manual refresh can produce persisted screening results from the newly cached data.
- Chip peak unavailability remains row-level and non-fatal.
- Chart data for a selected row uses the same screening run and refresh job as the table values.
- The page no longer requires the user to manually reload after a successful full refresh workflow.
</success_criteria>
