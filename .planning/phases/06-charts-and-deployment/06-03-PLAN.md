---
phase: 06-charts-and-deployment
plan: "06-03"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - README.md
  - .env.example
  - package.json
  - package-lock.json
  - playwright.config.ts
  - tests/smoke/app-smoke.spec.ts
  - tests/smoke/seed-smoke-db.ts
  - src/middleware.ts
  - src/proxy.ts
  - tests/auth/access-gate.test.ts
autonomous: true
requirements: [DEPL-01, DEPL-03]
must_haves:
  truths:
    - "DEPL-01: 项目必须说明云端自托管启动方式和必要环境变量。"
    - "DEPL-03: 项目必须提供类型检查、算法单元测试和页面冒烟检查命令。"
    - "Access protection must remain server-side and must not expose APP_PASSWORD or TUSHARE_TOKEN."
  artifacts:
    - path: "README.md"
      provides: "self-hosting and verification instructions"
    - path: "playwright.config.ts"
      provides: "browser smoke test configuration"
---

## Phase Goal

**As a** user, **I want** clear deployment and verification commands, **so that** I can run the finished app on a cloud server and confirm the main page works before relying on it.

<objective>
补齐自托管说明、环境变量样例、Next 16 访问保护命名迁移和 Playwright 页面冒烟检查，形成 v1 可运行收尾。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-charts-and-deployment/06-CONTEXT.md
@.planning/phases/06-charts-and-deployment/06-RESEARCH.md
@.planning/phases/06-charts-and-deployment/06-02-PLAN.md
@README.md

<dependencies>
- Execute after the table/chart user path is present so smoke tests can cover the final v1 screen.
- Smoke tests must use seeded local SQLite data and must not call Tushare/tinyshare.
- Do not add Docker, Redis, PostgreSQL, schedulers, or multi-user auth in v1.
</dependencies>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Playwright smoke test scaffold</name>
  <files>package.json, package-lock.json, playwright.config.ts, tests/smoke/app-smoke.spec.ts, tests/smoke/seed-smoke-db.ts</files>
  <behavior>
    - `@playwright/test` is installed as a dev dependency.
    - A smoke command launches the app through Playwright `webServer`.
    - The smoke database is seeded locally with refresh, screening, chip, and daily bar data.
    - The smoke test logs in with a test `APP_PASSWORD` and verifies table plus chart presence.
    - No smoke path requires a real Tushare token or network provider call.
  </behavior>
  <action>Add a deterministic browser smoke path for the finished protected workspace.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/auth/access-gate.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run smoke</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate access gate from middleware to proxy</name>
  <files>src/middleware.ts, src/proxy.ts, tests/auth/access-gate.test.ts</files>
  <behavior>
    - Access protection behavior remains unchanged for page routes and `/api/*`.
    - Tests import the new proxy entrypoint and continue to verify unauthenticated redirects/API 401s.
    - Login, static assets, and Next internals remain public as before.
  </behavior>
  <action>Rename the Next.js request boundary to the current `proxy.ts` convention and update tests.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/auth/access-gate.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run build</automated>
  </verify>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document cloud self-hosting and environment variables</name>
  <files>README.md, .env.example</files>
  <behavior>
    - README lists required env vars: `APP_PASSWORD`, `TUSHARE_TOKEN`, `TUSHARE_PROVIDER`, and `REFRESH_DB_PATH`.
    - README explains tinyshare setup without embedding or echoing any real authorization code.
    - README includes install/build/start commands for a self-hosted Node process.
    - README includes a minimal PM2 or systemd option, choosing the simpler one for a personal cloud server.
    - `.env.example` provides placeholders only.
  </behavior>
  <action>Update operational documentation for local and cloud use without expanding product scope.</action>
  <verify>
    <manual>Review README for secret leakage and command accuracy.</manual>
  </verify>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Finalize verification commands</name>
  <files>package.json, README.md</files>
  <behavior>
    - `npm run verify` still covers typecheck, lint, unit tests, and production build.
    - A separate `npm run smoke` command covers the page smoke check.
    - README tells the user when to run `npm run verify` versus `npm run smoke`.
    - Verification commands are suitable for a cloud server after dependencies and Playwright browsers are installed.
  </behavior>
  <action>Make the verification surface explicit and run the full verification set.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run verify</automated>
    <automated>D:\NodeJS\npm.cmd run smoke</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- The project includes current deployment instructions and placeholder env configuration.
- The request access gate remains protected after the Next 16 proxy migration.
- Typecheck, unit tests, production build, and Playwright smoke checks are available as documented commands.
- No committed file contains real provider tokens or passwords.
</success_criteria>
