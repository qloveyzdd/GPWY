---
phase: 01-tushare-data-foundation
plan: "01-03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [src/lib/validation/chip-and-price-validation.ts, src/lib/validation/result-sanitizer.ts, src/components/status/status-workspace.tsx, src/app/api/validation/run/route.ts, tests/validation/chip-price-validation.test.ts, tests/ui/status-workspace.test.tsx]
autonomous: false
requirements: [DATA-03, DATA-04, CHIP-01, DEPL-02]
user_setup:
  - service: Tushare
    why: "Chip candidate and price-basis confirmation depends on the user's token and account permission."
    env_vars:
      - name: TUSHARE_TOKEN
        source: ".env.local on the server"
must_haves:
  truths:
    - "D-09: Tushare price-basis availability is tested before locking the basis."
    - "D-10: Front-adjusted prices are preferred when stable; otherwise unadjusted fallback is recorded."
    - "D-11: Status page displays price basis, reason, and fallback risk."
    - "D-12: Chip capability is marked blocked when official candidate data is unavailable or permission denied."
    - "D-13: No unverified chip peak estimate is produced."
    - "The app records a clear price-basis decision: front-adjusted when stable, otherwise unadjusted with risk reason."
    - "The app attempts chip candidate interfaces and records available, permission denied, empty, not supported, or blocked states."
    - "The status workspace shows token, stock basic, price basis, and chip candidate sections with sanitized copy."
    - "No chip peak estimate is produced when Tushare chip data is unavailable."
  artifacts:
    - path: "src/lib/validation/chip-and-price-validation.ts"
      provides: "Chip candidate and price-basis decision runner"
    - path: "src/lib/validation/result-sanitizer.ts"
      provides: "Final status snapshot sanitizer"
    - path: "src/components/status/status-workspace.tsx"
      provides: "Final Phase 1 status sections and blocked states"
  key_links:
    - from: "src/lib/validation/chip-and-price-validation.ts"
      to: "src/lib/tushare/client.ts"
      via: "candidate api_name calls"
    - from: "src/app/api/validation/run/route.ts"
      to: "src/lib/validation/chip-and-price-validation.ts"
      via: "validation runner composition"
    - from: "src/components/status/status-workspace.tsx"
      to: "validation snapshot schema"
      via: "renders sanitized price-basis and chip candidate fields"
---

## Phase Goal

**As a** personal maintainer, **I want to** see trustworthy price-basis and chip-candidate capability results, **so that** later screening and chip-peak work starts from measured Tushare data availability.

<objective>
Finalize Phase 1 by adding chip candidate validation, price-basis decision recording, complete sanitized status presentation, and real-token verification instructions.

Purpose: retire the largest data-source uncertainties before the refresh, screening, and chip-peak phases.
Output: chip/price validation runner, final status UI sections, blocked-state handling, and real-token verification checkpoint.
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tushare-data-foundation/01-CONTEXT.md
@.planning/phases/01-tushare-data-foundation/01-UI-SPEC.md
@.planning/phases/01-tushare-data-foundation/01-RESEARCH.md
@.planning/phases/01-tushare-data-foundation/01-VALIDATION.md
@.planning/phases/01-tushare-data-foundation/01-PATTERNS.md
@.planning/phases/01-tushare-data-foundation/01-02-SUMMARY.md

<interfaces>
Use contracts created by plans 01-01 and 01-02:
- Tushare client and endpoint registry from `src/lib/tushare/*`.
- Snapshot read/write helpers from `src/lib/validation-store.ts`.
- Protected validation route from `src/app/api/validation/run/route.ts`.

Create final Phase 1 contract:
- `runChipAndPriceValidation()` returns `{ priceBasis, chipCandidates, errors }` with sanitized states only.
- `sanitizeValidationSnapshot()` returns the exact shape rendered by `StatusWorkspace`.
</interfaces>
</context>

<decision_coverage>
- D-09: Test Tushare price-basis availability before locking the basis.
- D-10: Prefer front-adjusted prices when stable, otherwise record unadjusted fallback.
- D-11: Display selected price basis, reason, and fallback risk on the status page.
- D-12: Mark chip capability blocked when candidate interfaces are unavailable, insufficient, or permission denied.
- D-13: Do not create any estimated chip peak when official Tushare chip data is unavailable.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement chip candidate and price-basis decision runner</name>
  <files>src/lib/validation/chip-and-price-validation.ts, src/lib/validation/result-sanitizer.ts, src/app/api/validation/run/route.ts, tests/validation/chip-price-validation.test.ts</files>
  <behavior>
    - Price basis selects front-adjusted only when validation proves it is stable; otherwise unadjusted is selected with explicit risk reason.
    - Chip candidate validation attempts `cyq_chips`, `cyq_perf`, and an endpoint-registry-defined equivalent list.
    - Chip candidate results are recorded as available, permission_denied, empty, not_supported, or blocked.
    - No branch fabricates a chip peak price from unverified estimates.
  </behavior>
  <action>Implement D-09, D-10, D-11, D-12, D-13, DATA-03, DATA-04, CHIP-01, and DEPL-02. Extend the validation route so the stored snapshot combines config, stock_basic, price-basis, and chip candidate sections. Candidate chip endpoint names must come from `src/lib/tushare/endpoints.ts`, not scattered strings. The price-basis decision must save both machine-readable basis and human-readable reason/risk copy. When chip data is unavailable or permission denied, mark chip capability blocked or degraded and do not produce a chip peak field.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/validation/chip-price-validation.test.ts tests/validation/error-sanitizer.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <done>Price-basis selection and chip candidate states are deterministic under mocked Tushare responses, and unavailable chip data never creates an estimated chip peak.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete status workspace sections and safe copy</name>
  <files>src/components/status/status-workspace.tsx, tests/ui/status-workspace.test.tsx</files>
  <behavior>
    - UI displays token configuration, Tushare connection, stock basic sample, price basis, and chip candidate sections.
    - Status labels are `未验证`, `正常`, `警告`, `阻塞`.
    - Warning and blocked copy matches UI-SPEC for price fallback and chip unavailability.
    - Raw Tushare payload, token, headers, stack trace, and server path values are not rendered.
  </behavior>
  <action>Update the status workspace to consume the final sanitized snapshot shape from Task 1. Apply the UI-SPEC visual hierarchy, color roles, typography, copywriting, no nested card rule, and accessibility requirements. Ensure the primary button calls only the protected validation API and then refreshes displayed snapshot state. Long endpoint names and error categories must wrap within the layout.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/status-workspace.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run verify</automated>
  </verify>
  <done>Status workspace fully represents Phase 1 validation sections and safe copy while keeping sensitive values absent from rendered output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify real-token status workspace behavior</name>
  <files>src/components/status/status-workspace.tsx, src/app/api/validation/run/route.ts</files>
  <action>Confirm the completed protected status workspace against a real `.env.local` with `APP_PASSWORD` and `TUSHARE_TOKEN`.</action>
  <verify>Human follows the real-token verification steps and responds with approved or issues.</verify>
  <done>Real-token run confirms stock, price-basis, and chip candidate sections show safe success, warning, or blocked states.</done>
  <what-built>Protected status workspace with real-token validation for stock basics, price basis, and chip candidate endpoints.</what-built>
  <how-to-verify>
    1. Create `.env.local` with `APP_PASSWORD` and `TUSHARE_TOKEN` on the local/server environment.
    2. Run `D:\NodeJS\npm.cmd run dev`.
    3. Open the local app, enter the app password, and confirm the status workspace loads without an automatic Tushare call.
    4. Click `重新验证数据源`.
    5. Confirm stock basic status shows safe code/name sample or a sanitized Tushare error category.
    6. Confirm price-basis status shows selected basis, reason, and fallback risk when applicable.
    7. Confirm chip candidate status reports available, permission denied, empty, not supported, or blocked.
    8. Confirm no token, raw Tushare response, request headers, stack traces, or local paths are visible.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validation runner -> Tushare candidate endpoints | Account permission and endpoint support are external and untrusted. |
| External response -> price/chip decision | Remote data determines stored status and later phase readiness. |
| Snapshot -> status UI | Stored diagnostic data becomes user-visible text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | `src/lib/validation/chip-and-price-validation.ts` | mitigate | Validate response field shapes and record only recognized status enums. |
| T-01-10 | Information Disclosure | `src/lib/validation/result-sanitizer.ts` | mitigate | Strip raw payloads, request metadata, stack traces, and token-shaped strings before storage/UI. |
| T-01-11 | Repudiation | validation snapshot | mitigate | Store timestamp, interface name, status, and reason for every validation section. |
| T-01-12 | Elevation | `src/app/api/validation/run/route.ts` | mitigate | Reuse protected route guard from plan 01-02 for chip and price probes. |

</threat_model>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/validation/chip-price-validation.test.ts tests/ui/status-workspace.test.tsx`
- `D:\NodeJS\npm.cmd run verify`
- Human real-token checkpoint from Task 3.
</verification>

<success_criteria>
- Price basis decision is explicit, machine-readable, and explained in UI.
- Chip candidate interfaces are validated and status is recorded without estimates.
- Status workspace presents all Phase 1 required sections using sanitized summaries.
- Real-token verification confirms available, degraded, or blocked data-source status.
</success_criteria>

<output>
Create `.planning/phases/01-tushare-data-foundation/01-03-SUMMARY.md` when done.
</output>
