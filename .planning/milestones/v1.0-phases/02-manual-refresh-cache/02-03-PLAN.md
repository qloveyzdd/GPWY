---
phase: 02-manual-refresh-cache
plan: "02-03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/lib/tushare/endpoints.ts
  - src/lib/refresh/fetch-refresh-data.ts
  - src/lib/refresh/refresh-runner.ts
  - src/lib/refresh/refresh-store.ts
  - tests/refresh/fetch-refresh-data.test.ts
  - tests/refresh/refresh-runner.test.ts
autonomous: true
requirements: [DATA-02, REFR-03, REFR-05]
must_haves:
  truths:
    - "D-02-01: listed A-share stock basics are cached from stock_basic."
    - "D-02-02: daily bars include open, high, low, close, and vol."
    - "D-02-03: refresh caches the latest 60 trading dates with data."
    - "D-02-07: provider failures are stored as sanitized categories/summaries only."
  artifacts:
    - path: "src/lib/refresh/fetch-refresh-data.ts"
      provides: "Tushare/tinyshare-backed stock basic and 60-trading-day daily bar fetcher"
---

## Phase Goal

**As a** user, **I want** manual refresh to cache real A-share basics and recent daily bars, **so that** Phase 3 can screen from local SQLite data instead of calling Tushare during every calculation.

<objective>
Wire the refresh worker to real Tushare/tinyshare provider calls and cache stock basics plus the latest 60 trading dates of daily bars.
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-manual-refresh-cache/02-CONTEXT.md
@.planning/phases/02-manual-refresh-cache/02-RESEARCH.md
@.planning/phases/02-manual-refresh-cache/02-01-SUMMARY.md
@.planning/phases/02-manual-refresh-cache/02-02-SUMMARY.md
@.planning/phases/01-tushare-data-foundation/01-03-SUMMARY.md

<interfaces>
- Use `createTushareClient()` so REST and tinyshare provider paths both work.
- Extend `TUSHARE_ENDPOINTS.daily` fields to include OHLCV.
- Use refresh store writes from 02-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add provider fetcher tests</name>
  <files>tests/refresh/fetch-refresh-data.test.ts</files>
  <behavior>
    - Fetcher calls `stock_basic` once.
    - Fetcher queries `daily` by trade date until 60 trading dates with data are collected.
    - Empty dates are skipped.
    - Returned bars include open, high, low, close, and vol.
  </behavior>
  <action>Add tests using an injected `TushareClientLike` mock. Do not call real Tushare in tests.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/fetch-refresh-data.test.ts</automated>
  </verify>
  <done>Tests fail for missing real refresh fetcher behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement real refresh data fetcher</name>
  <files>src/lib/tushare/endpoints.ts, src/lib/refresh/fetch-refresh-data.ts, tests/refresh/fetch-refresh-data.test.ts</files>
  <behavior>
    - `stock_basic` uses listed stocks only.
    - `daily` supports trade-date queries and OHLCV fields.
    - The fetcher bounds lookback attempts so a holiday stretch cannot loop forever.
    - Errors propagate through existing safe Tushare error classification.
  </behavior>
  <action>Implement stock basic fetch, daily trade-date iteration, row mapping, and output normalization for cache writes.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/fetch-refresh-data.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <done>Fetcher tests pass and the endpoint registry exposes required daily fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire real fetcher into refresh runner</name>
  <files>src/lib/refresh/refresh-runner.ts, src/lib/refresh/refresh-store.ts, tests/refresh/refresh-runner.test.ts</files>
  <behavior>
    - Refresh runner creates a job, fetches real data, writes stock basics and daily bars, and marks success with counts.
    - Failure marks the job failed with sanitized summary.
    - Latest successful status reports cached stock count and daily bar count.
  </behavior>
  <action>Connect the default refresh worker to `createTushareClient()` and `fetchRefreshData()`, then persist results through the store.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/fetch-refresh-data.test.ts tests/refresh/refresh-runner.test.ts tests/refresh/refresh-store.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run verify</automated>
  </verify>
  <done>Manual refresh can populate the SQLite cache through the real provider path, while tests remain mocked.</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/refresh/fetch-refresh-data.test.ts tests/refresh/refresh-runner.test.ts tests/refresh/refresh-store.test.ts`
- `D:\NodeJS\npm.cmd run verify`
- Optional local real-token smoke: start dev server, click manual refresh, confirm status becomes succeeded or stores a sanitized failure.
</verification>

<success_criteria>
- Refresh writes listed stock basics.
- Refresh writes at least the latest 60 trading dates of daily OHLCV rows when provider data is available.
- Refresh status records success/failure counts and sanitized errors.
- Latest successful cache is available for Phase 3.
</success_criteria>

<output>
Create `.planning/phases/02-manual-refresh-cache/02-03-SUMMARY.md` when done.
</output>
