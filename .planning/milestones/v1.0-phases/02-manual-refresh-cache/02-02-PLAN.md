---
phase: 02-manual-refresh-cache
plan: "02-02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/refresh/refresh-runner.ts
  - src/app/api/refresh/run/route.ts
  - src/app/api/refresh/status/route.ts
  - src/components/status/status-workspace.tsx
  - tests/refresh/refresh-runner.test.ts
  - tests/ui/status-workspace.test.tsx
autonomous: true
requirements: [REFR-01, REFR-02, REFR-03, REFR-04]
must_haves:
  truths:
    - "D-02-04: refresh is initiated by a protected API route and runs server-side only."
    - "D-02-05: duplicate browser clicks do not start multiple jobs."
    - "D-02-10: the existing protected status workspace gains manual refresh controls/status."
    - "D-02-12: running refresh status is visible through polling."
  artifacts:
    - path: "src/lib/refresh/refresh-runner.ts"
      provides: "Manual refresh controller with injectable worker and locking"
    - path: "src/app/api/refresh/run/route.ts"
      provides: "Protected POST endpoint to start a refresh"
    - path: "src/app/api/refresh/status/route.ts"
      provides: "Protected GET endpoint for latest refresh status"
---

## Phase Goal

**As a** user, **I want** to start a manual refresh and see its status, **so that** I can control API usage and know whether cached data is current.

<objective>
Build the refresh controller, protected refresh API routes, and status workspace controls using the store from 02-01.
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-manual-refresh-cache/02-CONTEXT.md
@.planning/phases/02-manual-refresh-cache/02-RESEARCH.md
@.planning/phases/02-manual-refresh-cache/02-01-SUMMARY.md

<interfaces>
- Use the refresh store created in 02-01.
- Reuse `getSession()` in API routes.
- Existing status page remains the first protected screen.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add refresh controller tests</name>
  <files>tests/refresh/refresh-runner.test.ts</files>
  <behavior>
    - Starting a refresh creates or returns a running job.
    - Duplicate start while running reports the existing job.
    - Worker success marks job succeeded.
    - Worker failure stores a sanitized failed job.
  </behavior>
  <action>Add tests with an injected worker so no real Tushare calls happen in this plan.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/refresh-runner.test.ts</automated>
  </verify>
  <done>Tests fail for missing refresh runner behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement refresh runner and protected API routes</name>
  <files>src/lib/refresh/refresh-runner.ts, src/app/api/refresh/run/route.ts, src/app/api/refresh/status/route.ts, tests/refresh/refresh-runner.test.ts</files>
  <behavior>
    - POST `/api/refresh/run` requires authentication.
    - GET `/api/refresh/status` requires authentication.
    - Starting while running returns the active job/status rather than starting another worker.
    - Errors are classified/sanitized before persistence.
  </behavior>
  <action>Implement server-side refresh runner and routes. The worker can be a placeholder injectable function until 02-03 wires real provider fetching.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/refresh-runner.test.ts tests/auth/access-gate.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <done>Refresh route and runner tests pass without real provider calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add manual refresh controls to status workspace</name>
  <files>src/components/status/status-workspace.tsx, tests/ui/status-workspace.test.tsx</files>
  <behavior>
    - UI shows latest refresh status and latest successful refresh time.
    - UI has a manual refresh button.
    - Button disables or shows running state while refresh is active.
    - UI polls status while running.
    - Sensitive values are never rendered.
  </behavior>
  <action>Extend `StatusWorkspace` to accept initial refresh status, call `/api/refresh/run`, poll `/api/refresh/status`, and render status bands consistent with the existing UI style.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/status-workspace.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run verify</automated>
  </verify>
  <done>Status workspace exposes manual refresh without breaking existing data-source validation.</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/refresh/refresh-runner.test.ts tests/ui/status-workspace.test.tsx`
- `D:\NodeJS\npm.cmd run verify`
</verification>

<success_criteria>
- User can click a refresh button.
- Duplicate refresh attempts do not start multiple jobs.
- Page shows latest refresh status and last success time.
- Refresh APIs remain protected.
</success_criteria>

<output>
Create `.planning/phases/02-manual-refresh-cache/02-02-SUMMARY.md` when done.
</output>
