---
phase: 05-results-table-experience
plan: "05-02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/results/results-table.tsx
  - src/components/status/status-workspace.tsx
  - src/lib/results/results-types.ts
  - tests/ui/results-table.test.tsx
autonomous: true
requirements: [UI-03, UI-04]
must_haves:
  truths:
    - "D-05-01: 表格默认按当前价/区间高点比例升序排列。"
    - "D-05-02: UI 必须区分无匹配股票、刷新/筛选失败或数据不可用、以及筹码峰不可用。"
    - "D-05-03: 筹码峰不可用是行级状态，不能隐藏已入选股票。"
---

## Phase Goal

**As a** user, **I want** to sort the results and understand empty or unavailable states, **so that** I can interpret the latest screening output without confusing data gaps with no matches.

<objective>
在结果表格中加入关键指标排序，并补齐无结果、失败/不可用、筹码峰不可用三类状态表达。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-results-table-experience/05-CONTEXT.md
@.planning/phases/05-results-table-experience/05-RESEARCH.md
@.planning/phases/05-results-table-experience/05-01-PLAN.md

<dependencies>
- Execute after 05-01 has introduced `ResultsSnapshot` and `ResultsTable`.
- Do not add a table library unless implementation proves local React state cannot satisfy UI-03.
</dependencies>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sorting behavior tests</name>
  <files>tests/ui/results-table.test.tsx</files>
  <behavior>
    - Default order is current price / interval high ratio ascending.
    - Users can sort by current/high ratio.
    - Users can sort by drawdown.
    - Users can sort by chip peak price.
    - Active sort state is visible through accessible table header controls.
  </behavior>
  <action>Add component tests for the sortable metrics before implementing controls.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/results-table.test.tsx</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sortable table headers</name>
  <files>src/components/results/results-table.tsx</files>
  <behavior>
    - Use existing shadcn table primitives and local React state.
    - Use compact icon buttons or header buttons with accessible labels.
    - Sorting remains deterministic when values are equal by falling back to stock code.
    - Missing chip peak prices sort after available numeric prices.
  </behavior>
  <action>Add sorting controls for the three required metrics without introducing pagination or filtering.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/results-table.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement empty and unavailable state semantics</name>
  <files>src/components/results/results-table.tsx, src/components/status/status-workspace.tsx, src/lib/results/results-types.ts, tests/ui/results-table.test.tsx</files>
  <behavior>
    - `empty` communicates that the latest successful screening has no matching stocks.
    - `unavailable` communicates that screening data is not available or cannot be read.
    - Row-level chip `blocked`, `failed`, and `missing` states remain distinct from page-level unavailable state.
    - The UI does not expose raw provider payloads, tokens, or stack traces.
  </behavior>
  <action>Render clear state panels/rows for the required conditions while keeping matched rows visible.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/results-table.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run verify</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- UI-03 sorting works for ratio, drawdown, and chip peak price.
- UI-04 states are visually and semantically distinct.
- Chip peak blocked/unavailable does not remove matched stocks from the table.
- Full project verification passes after implementation.
</success_criteria>
