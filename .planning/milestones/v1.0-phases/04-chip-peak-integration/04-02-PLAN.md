---
phase: 04-chip-peak-integration
plan: "04-02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/chip/chip-store.ts
  - src/lib/chip/chip-runner.ts
  - tests/chip/chip-store.test.ts
  - tests/chip/chip-runner.test.ts
autonomous: true
requirements: [CHIP-03, CHIP-04, REFR-05]
---

## Phase Goal

**As a** maintainer, **I want** chip peaks persisted for latest screening results, **so that** Phase 5 can render the required chip peak column or clear blocked state.

<objective>
Fetch official `cyq_chips` data per screened stock, extract chip peaks, and persist succeeded/blocked/failed enrichment results.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add chip store and runner tests</name>
  <files>tests/chip/chip-store.test.ts, tests/chip/chip-runner.test.ts</files>
  <behavior>
    - Store writes and reads latest chip peak run/results.
    - Runner reads latest screening results.
    - Runner queries `cyq_chips` by `ts_code` and `trade_date`.
    - Permission or missing-config failures persist blocked sanitized results.
  </behavior>
  <action>Add tests with temporary SQLite and mocked `TushareClientLike`.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-store.test.ts tests/chip/chip-runner.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement chip peak integration</name>
  <files>src/lib/chip/chip-store.ts, src/lib/chip/chip-runner.ts</files>
  <behavior>
    - Uses `createTushareClient()` by default and injectable clients in tests.
    - References latest screening run id.
    - Persists per-stock `succeeded`, `blocked`, or `failed` status.
    - Never stores raw token/provider payloads.
  </behavior>
  <action>Implement persistence and provider-backed runner using the parser from 04-01.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-peak.test.ts tests/chip/chip-store.test.ts tests/chip/chip-runner.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run verify</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Latest screening results can be enriched with official chip peaks.
- Blocked chip state is persisted when official data cannot be used.
- No unverified chip estimate is introduced.
</success_criteria>
