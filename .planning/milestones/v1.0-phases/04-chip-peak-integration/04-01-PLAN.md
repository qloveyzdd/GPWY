---
phase: 04-chip-peak-integration
plan: "04-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/chip/chip-types.ts
  - src/lib/chip/chip-peak.ts
  - tests/chip/chip-peak.test.ts
autonomous: true
requirements: [CHIP-02, CHIP-04]
---

## Phase Goal

**As a** maintainer, **I want** a pure chip distribution parser, **so that** official `cyq_chips` data can produce a deterministic chip peak without provider or database coupling.

<objective>
Implement chip distribution row mapping and highest-percent peak extraction.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add chip peak parser tests</name>
  <files>tests/chip/chip-peak.test.ts</files>
  <behavior>
    - Maps `cyq_chips` table rows to typed distribution records.
    - Extracts the highest-percent price for the latest trade date.
    - Uses deterministic lower-price tie-break when percent ties.
    - Empty or malformed official rows fail instead of estimating.
  </behavior>
  <action>Add offline unit tests with mocked Tushare tables.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-peak.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement chip peak parser</name>
  <files>src/lib/chip/chip-types.ts, src/lib/chip/chip-peak.ts</files>
  <behavior>
    - No provider calls.
    - No volume/price approximation.
    - Return extraction source `cyq_chips_highest_percent`.
  </behavior>
  <action>Implement pure mapping and extraction functions.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/chip/chip-peak.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Chip peak extraction is deterministic and tested.
- Parser only accepts official chip distribution rows.
- Empty/malformed rows do not produce fake peaks.
</success_criteria>
