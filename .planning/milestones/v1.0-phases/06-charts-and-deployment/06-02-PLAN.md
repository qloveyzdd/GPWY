---
phase: 06-charts-and-deployment
plan: "06-02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - package.json
  - package-lock.json
  - src/components/charts/stock-kline-chart.tsx
  - src/components/results/results-table.tsx
  - src/components/status/status-workspace.tsx
  - tests/ui/results-table.test.tsx
  - tests/ui/stock-kline-chart.test.tsx
autonomous: true
requirements: [CHRT-01, CHRT-02, CHRT-03, CHRT-04, CHRT-05, CHRT-06]
must_haves:
  truths:
    - "D-06-04: 图表以内联方式出现在表格下方，点击表格行切换选中股票。"
    - "D-06-05: 有结果时默认选中当前表格排序下的第一行。"
    - "D-06-06: 主图必须是 K-line/candlestick，不是收盘价折线图。"
    - "D-06-07: 图表必须叠加 MA20、MA60、区间高点、85% 阈值和可用筹码峰。"
    - "D-06-08: v1 不增加单独成交量子图。"
    - "D-06-09: 交互仅包含 tooltip、legend toggles 和自动 60 日聚焦。"
    - "CHRT-06: 图表数据必须复用持久化筛选/筹码结果中的关键数值。"
  artifacts:
    - path: "src/components/charts/stock-kline-chart.tsx"
      provides: "client-side ECharts K-line rendering"
    - path: "src/components/results/results-table.tsx"
      provides: "row selection and inline chart placement"
---

## Phase Goal

**As a** user, **I want** to click a stock row and inspect its 60-trading-day K-line chart, **so that** I can visually confirm why it matched the downtrend screen.

<objective>
在现有结果表下方加入内联 K 线图，展示价格、MA20、MA60、区间高点、85% 阈值和筹码峰覆盖线，并保持与表格排序和行选择一致。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-charts-and-deployment/06-CONTEXT.md
@.planning/phases/06-charts-and-deployment/06-RESEARCH.md
@.planning/phases/06-charts-and-deployment/06-01-PLAN.md

<dependencies>
- Execute after 06-01 has added the selected-stock chart API/helper.
- Use direct `echarts` dependency unless implementation proves a wrapper is necessary.
- Keep chart UI consistent with the existing quiet dashboard style; do not introduce a landing-page or marketing layout.
</dependencies>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ECharts dependency and chart component tests</name>
  <files>package.json, package-lock.json, tests/ui/stock-kline-chart.test.tsx</files>
  <behavior>
    - The project depends on `echarts`.
    - Tests can render the chart component with a mocked ECharts module or a DOM-safe adapter.
    - Empty, loading, unavailable, and ready chart states are testable without a browser canvas.
  </behavior>
  <action>Install `echarts` and add component tests that define the expected chart states before implementing the component.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/stock-kline-chart.test.tsx</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the client-side K-line chart component</name>
  <files>src/components/charts/stock-kline-chart.tsx</files>
  <behavior>
    - The chart uses candlestick series for OHLC data.
    - MA20 and MA60 render as line series.
    - Interval high, 85% threshold, and available chip peak render as named horizontal overlays.
    - Chip peak `blocked`, `failed`, or `missing` states show a clear non-fake state instead of drawing a line.
    - The component disposes ECharts instances and resizes responsively.
  </behavior>
  <action>Create a focused client component that transforms `ChartSnapshot` into ECharts options.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/stock-kline-chart.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add row selection and inline chart placement</name>
  <files>src/components/results/results-table.tsx, src/components/status/status-workspace.tsx, tests/ui/results-table.test.tsx</files>
  <behavior>
    - Ready results default-select the first row under the current table ordering.
    - Clicking a table row changes the selected stock.
    - Selection remains visible through accessible row state.
    - The chart appears inline below the table, not on a separate route and not in an extra action column.
    - Sorting either preserves the current selected row when present or selects the new first row; choose the simpler behavior and document it in the summary.
  </behavior>
  <action>Lift or localize selection in the table component according to the least invasive existing component structure.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/results-table.test.tsx tests/ui/stock-kline-chart.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Load selected chart data from the API</name>
  <files>src/components/charts/stock-kline-chart.tsx, src/components/results/results-table.tsx, tests/ui/results-table.test.tsx</files>
  <behavior>
    - The selected stock triggers a fetch to the chart API from 06-01.
    - Loading and API failure states do not collapse the table layout.
    - Successful chart data displays the selected code/name and key overlay labels.
    - The chart container has stable responsive dimensions on desktop and mobile widths.
  </behavior>
  <action>Wire selection to chart data loading and render the chart panel below the table body.</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/results-table.test.tsx tests/ui/stock-kline-chart.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run verify</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- A user can select any result row and see the selected stock's 60-day K-line chart inline.
- Chart overlays include MA20, MA60, interval high, 85% threshold, and chip peak when available.
- Chart values are sourced from the same persisted result data as the table.
- The UI remains usable when chip peak is unavailable or chart API data is not found.
</success_criteria>
