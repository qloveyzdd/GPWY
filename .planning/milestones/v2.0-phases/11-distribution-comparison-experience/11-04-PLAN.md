---
phase: 11-distribution-comparison-experience
plan: "11-04"
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - tests/smoke/app-smoke.spec.ts
  - tests/smoke/seed-smoke-db.ts
  - tests/results/chart-data.test.ts
  - tests/ui/results-table.test.tsx
  - tests/ui/stock-kline-chart.test.tsx
autonomous: true
requirements: [UI-05, CHRT-07, CHRT-08, CHRT-09, CHRT-10, CHRT-11]
user_setup: []
must_haves:
  truths:
    - "D-11-01/D-11-02/D-11-03/D-11-04/D-11-05/D-11-06: smoke 必须覆盖行内展开、K 线在上、双分布在下、previous/latest 顺序和无联动的关键路径。"
    - "D-11-07/D-11-08/D-11-09/D-11-10: 最终验证必须证明双图为完整分布、共享尺度、仅图内最大档位标记且无 Top-N 截断。"
    - "D-11-11/D-11-12/D-11-13/D-11-14: smoke/组件测试必须覆盖单日不可用隔离、脱敏说明、missing 正常空状态和无总体部分可用徽标。"
    - "D-11-15/D-11-16/D-11-17/D-11-18: smoke/组件测试必须覆盖表格无筹码峰列、无筹码峰排序、无状态徽标、无新增查看按钮或 hover 提示。"
    - "UI-05/CHRT-07/CHRT-08/CHRT-09/CHRT-10/CHRT-11: Phase 11 所有 requirements 必须在 focused tests、typecheck、verify 和 smoke 中至少有一条验证路径。"
  artifacts:
    - path: "tests/smoke/app-smoke.spec.ts"
      provides: "Browser key-path validation for simplified table and dual-day distribution detail"
    - path: "tests/smoke/seed-smoke-db.ts"
      provides: "Smoke seed with latest/previous succeeded and blocked distribution states"
    - path: ".planning/phases/11-distribution-comparison-experience/11-VALIDATION.md"
      provides: "Phase validation map used during execution"
  key_links:
    - from: "tests/smoke/seed-smoke-db.ts"
      to: "tests/smoke/app-smoke.spec.ts"
      via: "seeded 000001.SZ has succeeded previous/latest distributions and 000002.SZ has blocked distribution cards"
    - from: "tests/smoke/app-smoke.spec.ts"
      to: "src/components/results/results-table.tsx"
      via: "browser verifies no chip peak table surface"
    - from: "tests/smoke/app-smoke.spec.ts"
      to: "src/components/charts/stock-kline-chart.tsx"
      via: "browser verifies inline K-line and dual distribution charts"
---

<objective>
收口 Phase 11 的端到端体验验证：更新 smoke 关键路径，确认测试种子覆盖双日成功和单日不可用，并运行 focused tests、typecheck、生产验证和浏览器 smoke。

Purpose: 前三项计划分别改 DTO、表格和图表；本计划负责证明它们在真实受保护工作台路径中组合正确。
Output: 更新后的 smoke 断言、必要的 smoke seed 微调、全阶段验证通过。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-distribution-comparison-experience/11-CONTEXT.md
@.planning/phases/11-distribution-comparison-experience/11-RESEARCH.md
@.planning/phases/11-distribution-comparison-experience/11-VALIDATION.md
@.planning/phases/11-distribution-comparison-experience/11-02-SUMMARY.md
@.planning/phases/11-distribution-comparison-experience/11-03-SUMMARY.md
@tests/smoke/app-smoke.spec.ts
@tests/smoke/seed-smoke-db.ts
@package.json

<interfaces>
- Smoke login flow remains unchanged: visit `/`, redirect `/login`, fill `APP_PASSWORD` fallback.
- `tests/smoke/seed-smoke-db.ts` already has `chip_distribution_*` rows for 000001.SZ latest/previous succeeded and 000002.SZ blocked.
- Browser smoke should not depend on old `chip_peak_*` rows for assertions.
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-11-10: component tests pass but protected browser path still shows old chip peak UI | medium | Smoke asserts no “筹码峰价格” column and no “筹码峰1/2/3” detail badges. |
| T-11-11: smoke seed still relies on legacy chip peak rows, hiding distribution DTO bugs | medium | Smoke assertions target dual distribution dates/cards; seed distribution rows are verified or adjusted. |
| T-11-12: Playwright dependency unavailable in local environment | low | If browser cannot run due missing browser binary, document exact blocker in SUMMARY; otherwise smoke must pass. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 更新浏览器 smoke 到双分布详情路径</name>
  <files>tests/smoke/app-smoke.spec.ts</files>
  <read_first>tests/smoke/app-smoke.spec.ts, tests/smoke/seed-smoke-db.ts, src/components/results/results-table.tsx, src/components/charts/stock-kline-chart.tsx</read_first>
  <behavior>
    - 工作台仍受登录保护。
    - 页面不显示全量重建入口。
    - 结果表格不显示“筹码峰价格”列。
    - 展开 000001.SZ 后显示 K 线和 previous/latest 两张分布图。
    - 展开 000002.SZ 后显示单日不可用卡片，页面仍可用。
  </behavior>
  <action>将 `app-smoke.spec.ts` 测试名和断言从 “top three chip peaks” 改为 “dual chip distributions”。保留登录和无全量重建入口断言。增加 `await expect(page.getByText("筹码峰价格")).toHaveCount(0)` 和筹码峰 badge 不存在断言。点击可用行后断言 `000001.SZ K线图` canvas 存在，并断言 “前一有效交易日 20260059”、“最新有效交易日 20260060” 可见；如图表 canvas 可稳定定位，断言至少 3 个 chart canvas。点击 blocked 行后断言对应不可用状态/类别可见，且没有 token/path 字样。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run smoke -- tests/smoke/app-smoke.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - smoke 不再断言 “筹码峰1/2/3”
    - smoke 断言表格没有 “筹码峰价格”
    - smoke 覆盖 available row 的 previous/latest 分布日期
    - smoke 覆盖 blocked row 的不可用卡片
  </acceptance_criteria>
  <done>浏览器关键路径验证 Phase 11 新体验。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 检查并必要时微调 smoke seed 分布数据</name>
  <files>tests/smoke/seed-smoke-db.ts, tests/smoke/app-smoke.spec.ts</files>
  <read_first>tests/smoke/seed-smoke-db.ts, src/lib/results/chart-data.ts, src/lib/chip/chip-store.ts, tests/results/chart-data.test.ts</read_first>
  <behavior>
    - 000001.SZ 有 latest succeeded 和 previous succeeded。
    - 000001.SZ latest/previous 价格档位不完全相同，能验证共享 priceLevels。
    - 000002.SZ 有 blocked latest/previous，能验证不可用卡。
    - smoke 不依赖旧 chip_peak rows 才能通过双分布断言。
  </behavior>
  <action>优先复用现有 seed：它已经写入 `chip_distribution_runs`、`chip_distribution_statuses` 和 `chip_distribution_levels`。如果 11-01/11-03 实现需要更稳定的可见标签，可只做最小微调，例如增加 previous 分布一个 distinct price level 或补充 errorSummary 中文文案。不要删除 legacy chip peak seed，除非实现已经确认完全不需要；但 smoke 断言不得依赖 legacy chip peak 展示。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/results/chart-data.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run smoke -- tests/smoke/app-smoke.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `tests/smoke/seed-smoke-db.ts` 包含 `chip_distribution_runs`
    - 000001.SZ latest=`20260060`、previous=`20260059` 的 status 均为 succeeded
    - 000002.SZ 至少一个 distribution target status 为 blocked
    - smoke 断言不再读取或显示旧 chip peak rows
  </acceptance_criteria>
  <done>smoke 数据能真实驱动双分布图路径。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 执行 Phase 11 全阶段验证并清理旧 UI 残留</name>
  <files>src/lib/results/chart-types.ts, src/lib/results/chart-data.ts, src/components/results/results-table.tsx, src/components/charts/stock-kline-chart.tsx, tests/results/chart-data.test.ts, tests/ui/results-table.test.tsx, tests/ui/stock-kline-chart.test.tsx, tests/smoke/app-smoke.spec.ts</files>
  <read_first>.planning/phases/11-distribution-comparison-experience/11-VALIDATION.md, package.json, src/components/results/results-table.tsx, src/components/charts/stock-kline-chart.tsx, tests/smoke/app-smoke.spec.ts</read_first>
  <behavior>
    - focused tests、typecheck、lint/build 组合验证通过。
    - old chip peak UI 不残留在 results table 或 stock detail chart。
    - 如果 full verify 或 smoke 因环境失败，SUMMARY 写明精确命令和 blocker。
  </behavior>
  <action>按 `11-VALIDATION.md` 运行 focused Vitest、`D:\NodeJS\npm.cmd run typecheck`、`D:\NodeJS\npm.cmd run verify` 和 smoke。用 `rg` 检查旧 UI 残留：`results-table.tsx` 不应出现 `chipPeakPrice`、`筹码峰价格`；`stock-kline-chart.tsx` 不应出现旧筹码峰 badge/markLine 逻辑。若仍有 backend compatibility 的 `chipPeak*` 类型或 tests，这是允许的，但不能在 Phase 11 UI 文件中驱动展示。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/results/chart-data.test.ts tests/ui/results-table.test.tsx tests/ui/stock-kline-chart.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run verify</automated>
    <automated>D:\NodeJS\npm.cmd run smoke -- tests/smoke/app-smoke.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - focused Vitest exits 0
    - `D:\NodeJS\npm.cmd run verify` exits 0
    - smoke exits 0 or exact browser dependency blocker is documented
    - UI files no longer contain old chip peak table/chart display strings
  </acceptance_criteria>
  <done>Phase 11 可交付体验通过自动化和浏览器关键路径验证。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/results/chart-data.test.ts tests/ui/results-table.test.tsx tests/ui/stock-kline-chart.test.tsx`
- `D:\NodeJS\npm.cmd run typecheck`
- `D:\NodeJS\npm.cmd run verify`
- `D:\NodeJS\npm.cmd run smoke -- tests/smoke/app-smoke.spec.ts --reporter=line`
- `rg "chipPeakPrice|筹码峰价格|筹码峰[0-9]|chipPeaks|chipPeakState" src/components/results src/components/charts`
</verification>

<success_criteria>
- Browser smoke 覆盖表格无筹码峰列、行内 K 线和双日分布图。
- Browser smoke 覆盖单日不可用卡片且页面继续可用。
- focused tests、typecheck、verify 通过。
- 旧筹码峰 UI 展示从 Phase 11 表格和 K 线组件中清理完成。
</success_criteria>

<output>
完成后创建 `.planning/phases/11-distribution-comparison-experience/11-04-SUMMARY.md`。
</output>

