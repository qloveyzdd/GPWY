---
phase: 11-distribution-comparison-experience
plan: "11-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/results/chart-types.ts
  - src/lib/results/chart-data.ts
  - tests/results/chart-data.test.ts
autonomous: true
requirements: [CHRT-07, CHRT-08, CHRT-09, CHRT-10, CHRT-11]
user_setup: []
must_haves:
  truths:
    - "D-11-07/D-11-08/D-11-10: 单日分布 DTO 必须表达完整横向条形图数据，两日共享同一 priceLevels 和 maxPercent，不允许 Top-N 裁剪。"
    - "D-11-09/D-11-11/D-11-12/D-11-13/D-11-14: DTO 只在单日分布内部表达最大占比档位和单日状态；单日失败不改变整张 ChartSnapshot 的 ready 状态，不暴露敏感错误细节。"
    - "CHRT-07/CHRT-08/CHRT-09/CHRT-10: latest 和 previous 两个目标都必须有独立交易日期、状态、levels 和不可用原因。"
    - "CHRT-11: chart data 不再把旧前三筹码峰作为 K 线 overlay 的数据契约。"
  artifacts:
    - path: "src/lib/results/chart-types.ts"
      provides: "ChartSnapshot dual-day chip distribution DTO and shared scale contract"
    - path: "src/lib/results/chart-data.ts"
      provides: "Server-side aggregation from screening row, K-line bars, and Phase 10 chip distribution cache"
      exports: ["readLatestChartSnapshot"]
    - path: "tests/results/chart-data.test.ts"
      provides: "DTO coverage for dual-day distributions, shared scale, unavailable states, and no legacy chip overlay"
  key_links:
    - from: "src/lib/results/chart-data.ts"
      to: "src/lib/chip/chip-store.ts"
      via: "reads latest distribution run, per-run statuses, and stock-date distribution levels"
    - from: "src/components/charts/stock-kline-chart.tsx"
      to: "src/lib/results/chart-types.ts"
      via: "client consumes shared-scale distribution DTO produced by chart API"
---

<objective>
建立 Phase 11 的股票详情图表 DTO：`readLatestChartSnapshot()` 在保留 K 线、MA20/MA60、区间高点和 85% 阈值的同时，返回 previous/latest 两个完整筹码分布目标、准确交易日期、单日状态和共享尺度，并移除 K 线旧筹码峰 overlay 契约。

Purpose: 后续 UI 只有拿到同一尺度的完整分布数据，才能可靠地做双图对比；如果尺度留给组件各自自适应，会产生视觉误导。
Output: 双日筹码分布 ChartSnapshot 类型、服务端聚合逻辑和 chart-data 单元测试。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-distribution-comparison-experience/11-CONTEXT.md
@.planning/phases/11-distribution-comparison-experience/11-RESEARCH.md
@.planning/phases/10-dual-day-chip-distribution/10-CONTEXT.md
@.planning/phases/10-dual-day-chip-distribution/10-04-SUMMARY.md
@src/lib/results/chart-types.ts
@src/lib/results/chart-data.ts
@src/lib/results/results-snapshot.ts
@src/lib/chip/chip-store.ts
@src/lib/chip/chip-types.ts
@tests/results/chart-data.test.ts

<interfaces>
- `ChartSnapshot.status === "ready"` 仍表示 K 线和基础详情可用；单日分布失败不把 snapshot 改成 unavailable。
- `ChartOverlays` 保留 `intervalHighPrice`、`intervalHighTradeDate`、`threshold85Price`。
- 新增 `chipDistributions.previous` 和 `chipDistributions.latest`，目标顺序在 DTO 中稳定表达。
- 新增 shared scale，至少包含 `priceLevels: number[]` 和 `maxPercent: number`。
- 单日 DTO 至少包含 `targetKind`、`label`、`tradeDate`、`status`、`levels`、`maxLevel`、`errorCategory`、`errorSummary`。
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-11-01: 两张分布图各自用自适应价格/占比尺度，导致比较失真 | high | 在 `chart-data.ts` 计算 shared `priceLevels` 和 `maxPercent`，UI 强制共用。 |
| T-11-02: 单日 `blocked/failed/missing` 误把整个详情置为 unavailable | medium | ChartSnapshot 基础状态只由筛选/K 线决定；分布目标单独带 status。 |
| T-11-03: errorSummary 泄露 token、headers、本地路径或 provider 原始响应 | high | DTO 只透传 Phase 10 已脱敏字段，并在测试中断言敏感字样不存在。 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义双日完整分布 ChartSnapshot DTO</name>
  <files>src/lib/results/chart-types.ts, tests/results/chart-data.test.ts</files>
  <read_first>src/lib/results/chart-types.ts, src/lib/results/results-types.ts, src/lib/chip/chip-types.ts, .planning/phases/11-distribution-comparison-experience/11-CONTEXT.md</read_first>
  <behavior>
    - DTO 有 previous/latest 两个目标，不使用自然日“昨天/今天”描述。
    - DTO 有 shared priceLevels 和 maxPercent。
    - DTO 表达 succeeded/blocked/failed/missing 四种单日状态。
    - K 线 overlays 不再包含 `chipPeaks` 或 `chipPeakState`。
  </behavior>
  <action>在 `chart-types.ts` 新增 `ChartChipDistributionTargetKind`、`ChartChipDistributionStatus`、`ChartChipDistributionLevel`、`ChartChipDistributionPanel`、`ChartChipDistributionScale`、`ChartChipDistributions`。`ReadyChartSnapshot` 增加 `chipDistributions` 字段。`ChartOverlays` 删除旧 `chipPeaks`、`chipPeakState`，只保留价格 K 线 overlay。保留 `ResultRow` 兼容字段，不在本任务删除 backend compatibility。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `ReadyChartSnapshot` 类型要求 `chipDistributions.previous` 和 `chipDistributions.latest`
    - `ChartOverlays` 类型中不再出现 `chipPeaks` 或 `chipPeakState`
    - 单日状态联合类型包含 `succeeded`、`blocked`、`failed`、`missing`
    - DTO 字段名不要求暴露 `chip_distribution_*` 内部表名
  </acceptance_criteria>
  <done>前后端拥有双日分布图的稳定类型契约。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 从 Phase 10 缓存聚合 previous/latest 分布目标</name>
  <files>src/lib/results/chart-data.ts, tests/results/chart-data.test.ts</files>
  <read_first>src/lib/results/chart-data.ts, src/lib/chip/chip-store.ts, tests/results/chart-data.test.ts, tests/smoke/seed-smoke-db.ts</read_first>
  <behavior>
    - 读取与当前 screening run 匹配的 latest distribution run。
    - 当前股票 latest/previous 状态按 `targetKind` 独立映射。
    - `succeeded` 且有 tradeDate 时读取完整 levels。
    - 无 run、无 status 或无 previous 有效日时生成 `missing` panel。
  </behavior>
  <action>在 `readLatestChartSnapshot()` 中，得到 `screeningRunId` 和当前 `row` 后调用 `readLatestChipDistributionRun(screeningRunId)`；run 匹配时读取 `readChipDistributionStatusesForRun(run.id)` 并筛选当前 `tsCode`。对 latest/previous 分别构造 panel：status succeeded 时用 `readChipDistributionForDate(row.tsCode, status.tradeDate)` 读取所有 levels；blocked/failed/missing 时 levels 为空，并保留 `errorCategory/errorSummary`。如果缺少 status，latest 使用 `row.latestTradeDate` 生成 missing；previous 可从当前 bars 中定位 `row.latestTradeDate` 的前一条有效 bar，找不到则 tradeDate=null。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/results/chart-data.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - chart-data 测试中 latest=`20260060`、previous=`20260059` 均为 succeeded 且 levels 全量返回
    - 无 distribution run 时 snapshot 仍 ready，latest/previous panel 为 missing
    - latest blocked + previous succeeded 时 previous levels 仍正常返回
    - DTO 不包含 `TUSHARE_TOKEN`、`Authorization`、`C:\`、`REFRESH_DB_PATH` 或 provider 原始 payload
  </acceptance_criteria>
  <done>ChartSnapshot 能从现有缓存读出双日分布和单日状态。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 计算共享价格档位、占比尺度和单日最大占比档位</name>
  <files>src/lib/results/chart-data.ts, tests/results/chart-data.test.ts</files>
  <read_first>src/lib/results/chart-data.ts, tests/results/chart-data.test.ts, .planning/phases/11-distribution-comparison-experience/11-RESEARCH.md</read_first>
  <behavior>
    - shared `priceLevels` 是 successful panels 的全部价格档位并集，按价格升序。
    - shared `maxPercent` 是 successful panels 所有 levels 的最大 percent。
    - 单日 `maxLevel` 只代表该日最大占比档位。
    - 不裁剪 Top-N，不只保留前三峰。
  </behavior>
  <action>为 `chart-data.ts` 增加纯函数或局部逻辑：收集 previous/latest 中所有 succeeded panels 的 levels，生成去重升序 `priceLevels`；`maxPercent` 用最大 percent，若无成功 levels 则为 0。每个 panel 计算 `maxLevel`：按 `percent desc`，同 percent 时按 `price asc` 选择。测试用 latest 三档、previous 两档且价格不完全重合的数据，断言 `priceLevels` 长度为并集，`maxPercent` 来自两天最大值，且每个 panel 的 `levels` 没有被裁剪。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/results/chart-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `chipDistributions.scale.priceLevels` 包含 latest 和 previous 的全部价格档位并集
    - `chipDistributions.scale.maxPercent` 等于两天 levels 的最大占比
    - latest/previous panel 各自的 `maxLevel` 指向本日最大 percent 档位
    - 测试中存在 5 个不同价格档位时 DTO 不会只返回 3 个
  </acceptance_criteria>
  <done>双图比较所需的共享尺度由服务端稳定生成。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/results/chart-data.test.ts`
- `D:\NodeJS\npm.cmd run typecheck`
- `D:\NodeJS\npm.cmd run lint -- src/lib/results tests/results`
</verification>

<success_criteria>
- `readLatestChartSnapshot()` 返回 previous/latest 双日完整分布 DTO。
- 成功单日包含完整 levels、准确交易日期和本日 maxLevel。
- 两图共享 priceLevels 和 maxPercent。
- 单日不可用不影响另一日可用图，也不影响 K 线 snapshot ready。
- K 线 overlay 数据契约不再包含旧筹码峰。
</success_criteria>

<output>
完成后创建 `.planning/phases/11-distribution-comparison-experience/11-01-SUMMARY.md`。
</output>

