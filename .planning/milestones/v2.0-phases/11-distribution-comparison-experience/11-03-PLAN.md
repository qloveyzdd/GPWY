---
phase: 11-distribution-comparison-experience
plan: "11-03"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/charts/stock-kline-chart.tsx
  - tests/ui/stock-kline-chart.test.tsx
autonomous: true
requirements: [CHRT-07, CHRT-08, CHRT-09, CHRT-10, CHRT-11]
user_setup: []
must_haves:
  truths:
    - "D-11-02/D-11-03/D-11-04/D-11-05/D-11-06: 详情区 K 线在上，previous/latest 分布图在下；桌面并排、窄屏堆叠，previous 在左、latest 在右，等宽等高，无 hover/click 联动。"
    - "D-11-07/D-11-08/D-11-09/D-11-10: 单日分布图为横向条形图，共享价格和占比尺度，只标出本日最大占比档位，默认展示所有价格档位。"
    - "D-11-11/D-11-12/D-11-13/D-11-14: 单日失败/阻塞/缺失只影响对应卡片，missing 是正常空状态，详情头部不显示总体部分可用徽标。"
    - "CHRT-07/CHRT-08/CHRT-09/CHRT-10/CHRT-11: 股票详情展示 latest 和 previous 完整分布、准确交易日期、单日失败隔离，并移除 K 线前三筹码峰标记。"
  artifacts:
    - path: "src/components/charts/stock-kline-chart.tsx"
      provides: "K-line detail container plus dual chip distribution chart cards"
    - path: "tests/ui/stock-kline-chart.test.tsx"
      provides: "Component tests for no chip peak overlay, dual distribution charts, shared scale usage, and unavailable cards"
  key_links:
    - from: "src/components/charts/stock-kline-chart.tsx"
      to: "src/lib/results/chart-types.ts"
      via: "renders `chipDistributions.previous/latest` from ChartSnapshot"
---

<objective>
把 `StockKlineChart` 从“单张 K 线 + 筹码峰标记”改为“上方 K 线 + 下方 previous/latest 两张完整筹码分布图”的详情容器，并确保单日不可用不会影响另一日图表。

Purpose: 用户需要比较两个有效交易日的完整筹码分布，而不是继续看表格或 K 线上的前三筹码峰。
Output: K 线移除筹码峰 overlay、双日分布图卡片、单日不可用卡片和组件测试。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-distribution-comparison-experience/11-CONTEXT.md
@.planning/phases/11-distribution-comparison-experience/11-RESEARCH.md
@.planning/phases/11-distribution-comparison-experience/11-01-SUMMARY.md
@src/components/charts/stock-kline-chart.tsx
@src/lib/results/chart-types.ts
@tests/ui/stock-kline-chart.test.tsx

<interfaces>
- `StockKlineChart` 仍接收 `tsCode: string | null` 并从 `/api/results/chart/[tsCode]` fetch `ChartSnapshot`。
- K 线 ECharts option 的 `markLine.data` 只包含 “区间高点” 和 “85%阈值”。
- 分布图 card 使用 `snapshot.chipDistributions.scale.priceLevels` 和 `maxPercent`。
- 分布图 card 的可访问文本包含目标名称和实际 tradeDate，例如 “前一有效交易日 20260059”。
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-11-07: 多个 ECharts 实例共用同一个 ref/生命周期，导致 dispose/resize 错乱 | medium | 分布图拆成独立 chart card 子组件，每个 card 管理自己的 ref 和 instance。 |
| T-11-08: K 线旧筹码峰 badge 或 markLine 残留 | medium | 删除 `chipPeakColors`、chip peak markLine 分支和标题区筹码峰 badge，测试断言 markLine 只有两项。 |
| T-11-09: missing 被渲染成红色错误，误导为系统故障 | low | unavailable card 按 status 分 tone；missing 使用 muted/neutral 样式和正常空文案。 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 移除 K 线旧筹码峰 overlay 和标题 badge</name>
  <files>src/components/charts/stock-kline-chart.tsx, tests/ui/stock-kline-chart.test.tsx</files>
  <read_first>src/components/charts/stock-kline-chart.tsx, src/lib/results/chart-types.ts, tests/ui/stock-kline-chart.test.tsx, .planning/phases/11-distribution-comparison-experience/11-01-SUMMARY.md</read_first>
  <behavior>
    - K 线 markLine 只保留“区间高点”和“85%阈值”。
    - 详情头部不显示“筹码峰：无数据”或“筹码峰1/2/3” badge。
    - 图表 fetch/loading/unavailable/failed 基础状态不变。
  </behavior>
  <action>在 `stock-kline-chart.tsx` 删除 `chipPeakColors` 和 `snapshot.overlays.chipPeakState/chipPeaks` 分支。标题 badge 区只保留区间高点和 85% 阈值。更新 `stock-kline-chart.test.tsx` fixture 使用 11-01 的新 DTO；原 “renders a candlestick chart with MA and overlay lines” 用例改为断言无筹码峰文案，且 K 线 option `markLine.data.map(name)` 等于 `["区间高点", "85%阈值"]`。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/stock-kline-chart.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg "筹码峰|chipPeakColors|chipPeaks|chipPeakState" src/components/charts/stock-kline-chart.tsx` 不显示旧 K 线展示逻辑
    - K 线 markLine data 只有两条
    - loading/unavailable/failed 三个基础状态测试仍通过
    - 标题区没有总体“部分可用”徽标
  </acceptance_criteria>
  <done>K 线图不再承担筹码峰展示。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 渲染 previous/latest 双日完整分布横向条形图</name>
  <files>src/components/charts/stock-kline-chart.tsx, tests/ui/stock-kline-chart.test.tsx</files>
  <read_first>src/components/charts/stock-kline-chart.tsx, src/lib/results/chart-types.ts, tests/ui/stock-kline-chart.test.tsx</read_first>
  <behavior>
    - K 线图位于上方，分布图区域位于下方。
    - previous 在左，latest 在右；窄屏上下排列。
    - 两卡等宽等高。
    - 单日图为横向 bar：y 轴价格档位，x 轴占比。
    - 两图使用同一 `priceLevels` 和 `maxPercent`。
  </behavior>
  <action>在 `stock-kline-chart.tsx` 内拆出或新增内部组件，例如 `ChipDistributionCard`。成功状态下初始化独立 ECharts 实例：`xAxis.type="value"`，`xAxis.max=snapshot.chipDistributions.scale.maxPercent`，`yAxis.type="category"`，`yAxis.data=priceLevels.map(formatPrice)`，`series[0].type="bar"`，数据按 shared priceLevels 映射该 panel 的 percent，缺失档位用 0。布局使用 `mt-4 grid gap-4 lg:grid-cols-2` 或项目现有等价 Tailwind；卡片用固定最小高度，例如 `h-[320px]`，保证等高。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/stock-kline-chart.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 组件测试可看到 “前一有效交易日 20260059” 和 “最新有效交易日 20260060”
    - ECharts `init` 至少为 K 线、previous、latest 三个成功图创建实例
    - previous/latest bar option 的 `xAxis.max` 相同
    - previous/latest bar option 的 `yAxis.data` 相同且包含全部 shared priceLevels
    - 没有 Top-N 裁剪逻辑
  </acceptance_criteria>
  <done>详情区展示两个可比较的完整筹码分布图。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 单日最大占比标记和不可用卡片</name>
  <files>src/components/charts/stock-kline-chart.tsx, tests/ui/stock-kline-chart.test.tsx</files>
  <read_first>src/components/charts/stock-kline-chart.tsx, src/lib/results/chart-types.ts, tests/ui/stock-kline-chart.test.tsx</read_first>
  <behavior>
    - 成功图只在该分布图内部标出本日最大占比档位。
    - blocked/failed/missing 显示对应不可用卡片，不影响另一日图表。
    - missing 使用正常空状态样式。
    - 不可用卡片只显示 target、tradeDate、status、errorCategory、短说明。
  </behavior>
  <action>在 `ChipDistributionCard` 中，如果 panel.status !== `succeeded` 或 levels 为空，渲染 unavailable card。status label 建议：`missing=缺少数据`、`blocked=阻塞`、`failed=失败`；missing 用 muted tone，blocked/failed 用 warning/error tone。成功图用 `panel.maxLevel` 添加 markPoint、markLine 或自定义标签，必须只出现在该 card 的 bar option 内。测试覆盖 latest blocked + previous succeeded：previous 图仍初始化，latest card 显示 blocked/errorCategory，不显示 provider 原始 payload；另测 missing previous 显示中性空状态。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/stock-kline-chart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 单日最大占比标记只来自该 panel 的 `maxLevel`
    - latest blocked 时 previous bar 图仍渲染
    - missing previous 文案不使用红色错误类或 AlertTriangle 主导表达
    - 不可用卡片不显示 `TUSHARE_TOKEN`、`Authorization`、`C:\`、`REFRESH_DB_PATH`
  </acceptance_criteria>
  <done>部分失败状态在详情区被隔离且可理解。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/ui/stock-kline-chart.test.tsx tests/results/chart-data.test.ts`
- `D:\NodeJS\npm.cmd run typecheck`
- `D:\NodeJS\npm.cmd run lint -- src/components/charts tests/ui/stock-kline-chart.test.tsx`
</verification>

<success_criteria>
- K 线图没有旧前三筹码峰 markLine 或 badge。
- 详情区上方是 K 线，下方是 previous/latest 两张完整分布图。
- 两张分布图共享价格档位和占比尺度。
- 单日 unavailable card 不影响另一日成功图。
- 图表无 K 线与分布图 hover/click 联动。
</success_criteria>

<output>
完成后创建 `.planning/phases/11-distribution-comparison-experience/11-03-SUMMARY.md`。
</output>

