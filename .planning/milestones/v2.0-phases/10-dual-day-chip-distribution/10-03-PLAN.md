---
phase: 10-dual-day-chip-distribution
plan: "10-03"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/lib/chip/chip-store.ts
  - src/lib/results/results-types.ts
  - src/lib/results/results-snapshot.ts
  - src/lib/results/chart-data.ts
  - tests/results/results-snapshot.test.ts
  - tests/results/chart-data.test.ts
autonomous: true
requirements: [CHIP-07, CHIP-10]
user_setup: []
must_haves:
  truths:
    - "D-10-09: 完整筹码分布是唯一源数据，旧前三峰只作为兼容字段派生。"
    - "D-10-10: Phase 10 必须保持现有结果快照、表格筹码峰列和 K 线 overlay 不退化。"
    - "D-10-11/D-10-12: 旧兼容筹码峰只从 latest 目标日期派生；latest 失败时不得用 previous 成功冒充。"
    - "CHIP-07/CHIP-10: 兼容层读取完整分布和日期级状态，不读取旧前三峰作为事实源。"
  artifacts:
    - path: "src/lib/results/results-snapshot.ts"
      provides: "Latest results snapshot with compatibility chip peak projection"
      exports: ["readLatestResultsSnapshot"]
    - path: "src/lib/chip/chip-store.ts"
      provides: "Read APIs that project latest target distribution into legacy chip peak result rows"
    - path: "tests/results/results-snapshot.test.ts"
      provides: "Compatibility behavior tests for latest/previous distribution states"
  key_links:
    - from: "src/lib/results/results-snapshot.ts"
      to: "src/lib/chip/chip-store.ts"
      via: "snapshot reads latest distribution run and derives old chip peak fields"
    - from: "src/lib/results/chart-data.ts"
      to: "src/lib/results/results-snapshot.ts"
      via: "chart keeps existing overlay contract from ResultRow"
---

<objective>
把完整筹码分布投影为现有页面仍需要的旧筹码峰字段，确保 Phase 10 后结果表格和 K 线 overlay 不退化，同时不再把旧前三峰作为源数据。

Purpose: Phase 11 才会替换 UI；Phase 10 必须让用户继续看到当前页面，并保证旧字段语义只代表 latest 目标日期。
Output: 完整分布到旧 `ResultRow` 字段的兼容读取路径和结果/图表回归测试。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-dual-day-chip-distribution/10-CONTEXT.md
@.planning/phases/10-dual-day-chip-distribution/10-RESEARCH.md
@.planning/phases/10-dual-day-chip-distribution/10-PATTERNS.md
@.planning/phases/10-dual-day-chip-distribution/10-01-SUMMARY.md
@src/lib/results/results-types.ts
@src/lib/results/results-snapshot.ts
@src/lib/results/chart-data.ts
@src/lib/chip/chip-store.ts
@src/lib/chip/chip-peak.ts
@tests/results/results-snapshot.test.ts
@tests/results/chart-data.test.ts

<interfaces>
- `ResultRow` 保留 `chipPeakState`、`chipPeakPrice`、`chipPeakTradeDate`、`chipPeakSource`、`chipPeaks`、`chipPeakErrorCategory`、`chipPeakErrorSummary`。
- `readLatestResultsSnapshot()` 只使用与 latest screening run 匹配的 distribution run。
- latest target `succeeded` 时，旧 `chipPeaks` 来自 latest target 完整分布 top 3。
- latest target `blocked/failed/missing` 时，旧字段显示对应不可用状态，即使 previous succeeded。
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-10-05: previous 成功数据被错误投影成 latest 筹码峰 | medium | Snapshot 只读取 `targetKind="latest"` 的状态和 levels；测试覆盖 latest failed + previous succeeded。 |
| T-10-06: 旧 chip_peak run 与新 distribution run 混读导致口径不一致 | medium | Phase 10 snapshot 改为读取完整分布兼容投影；旧 `chip_peak_*` 不再作为 latest snapshot 源。 |
| T-10-07: 兼容 DTO 泄露内部状态表细节 | low | `ResultRow` 继续只暴露旧字段和脱敏错误摘要，不暴露 distribution table IDs。 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 从完整分布派生旧 ChipPeakResultRecord 兼容读取</name>
  <files>src/lib/chip/chip-store.ts, tests/results/results-snapshot.test.ts</files>
  <read_first>src/lib/chip/chip-store.ts, src/lib/chip/chip-peak.ts, src/lib/chip/chip-types.ts, tests/results/results-snapshot.test.ts</read_first>
  <behavior>
    - latest target succeeded 且有 levels 时，返回旧 `available` 语义所需的 top 3 peaks。
    - latest target failed/blocked/missing 时，返回旧不可用状态和脱敏原因。
    - previous target succeeded 不参与旧峰派生。
  </behavior>
  <action>在 `chip-store.ts` 增加读取函数，例如 `readCompatibleChipPeakResultsForDistributionRun(runId)` 或 `readLatestCompatibleChipPeakResults(screeningRunId)`。该函数对每只 tsCode 只读取 targetKind=`latest` 的状态；latest succeeded 时调用 `deriveChipPeakLevelsFromDistribution` 取 top 3，并填充旧 `ChipPeakResultRecord` shape；latest failed/blocked/missing 时返回 `chipPeakPrice=null`、`peaks=[]`、对应 errorCategory/errorSummary。不要从旧 `chip_peak_results` 或 previous target 读取兼容峰。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/results/results-snapshot.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 测试中 latest full distribution `[36.2:6.5, 35.8:4.2, 37.1:3.1]` 派生出三个旧 peaks
    - latest failed + previous succeeded 测试中 `chipPeakState="failed"` 且 `chipPeaks=[]`
    - `readCompatibleChipPeakResultsForDistributionRun` 不查询或不依赖 `chip_peak_results`
    - 兼容结果不包含 `chip_distribution_run_id` 或内部 status row id
  </acceptance_criteria>
  <done>旧结果 DTO 可以从完整分布唯一源派生。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 将结果快照切换到完整分布兼容投影</name>
  <files>src/lib/results/results-snapshot.ts, src/lib/results/results-types.ts, tests/results/results-snapshot.test.ts</files>
  <read_first>src/lib/results/results-snapshot.ts, src/lib/results/results-types.ts, src/lib/chip/chip-store.ts, tests/results/results-snapshot.test.ts</read_first>
  <behavior>
    - 最新 screening run 有匹配 distribution run 时，snapshot rows 使用兼容投影字段。
    - distribution run 属于旧 screening run 时，不 join 到最新结果。
    - blocked/failed/missing 行继续显示，结果表格不会隐藏股票。
    - `chipPeakRunId` 字段兼容保留；如果命名不变，可填 distribution run id，但不得混淆旧源。
  </behavior>
  <action>修改 `readLatestResultsSnapshot()`，将 `readLatestChipPeakRun()/readChipPeakResultsForRun()` 替换为读取最新 `chip_distribution_runs`。只有 `distributionRun.screeningRunId === screeningRun.id` 时才读取兼容结果。保留 `ResultsSnapshot.chipPeakRunId` 字段名以减少 UI 改动；其值可以是 distribution run id，但 `chipPeakSource` 仍为 `cyq_chips_highest_percent`。如果没有 matching distribution run，所有 row 的 chipPeakState 仍为 `missing`。更新 tests：旧 writeChipPeakRun fixture 改为新完整分布 fixture。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/results/results-snapshot.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `readLatestResultsSnapshot().rows[0].chipPeakState` 在 latest succeeded 时为 `available`
    - stale distribution run 不 join 到最新 screening run，row state 为 `missing`
    - blocked/failed/missing 三种日期级状态在 rows 中分别映射为同名旧 state
    - JSON 序列化结果不包含 `REFRESH_DB_PATH`、`TUSHARE_TOKEN`、SQLite 路径或 provider 原始 payload
  </acceptance_criteria>
  <done>结果快照继续服务现有 UI，但数据源已切到完整分布。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 保持 K 线 overlay 旧兼容行为</name>
  <files>src/lib/results/chart-data.ts, tests/results/chart-data.test.ts</files>
  <read_first>src/lib/results/chart-data.ts, src/lib/results/results-snapshot.ts, tests/results/chart-data.test.ts, tests/ui/stock-kline-chart.test.tsx</read_first>
  <behavior>
    - K 线 snapshot 继续返回 `overlays.chipPeaks` 和 `overlays.chipPeakState`。
    - latest succeeded 时 overlay 使用 latest top 3 peaks。
    - latest unavailable 时 overlay 不画假峰。
    - Phase 10 不新增双分布图 UI 数据结构到 chart snapshot。
  </behavior>
  <action>优先不改 `chart-data.ts` 结构；只更新测试 fixture 让其通过新完整分布 store 写入数据。如果 `ResultRow` 类型因兼容投影需要微调，同步保持 `readLatestChartSnapshot()` 的 overlay shape 不变：`intervalHighPrice`、`intervalHighTradeDate`、`threshold85Price`、`chipPeaks`、`chipPeakState`。不要在 Phase 10 增加双日分布图 chart series；该工作属于 Phase 11。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/results/chart-data.test.ts tests/ui/stock-kline-chart.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `readLatestChartSnapshot("000001.SZ").overlays.chipPeaks` 在 latest succeeded 时长度为 3
    - latest blocked/failed/missing 时 `overlays.chipPeaks` 等于 `[]`
    - `stock-kline-chart` 现有 chip peak marker 测试继续通过
    - `chart-data.ts` 未引入双分布图字段名，例如 `latestDistribution` 或 `previousDistribution`
  </acceptance_criteria>
  <done>Phase 10 不改变现有 K 线 UI 行为，并为 Phase 11 留出替换空间。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/results/results-snapshot.test.ts tests/results/chart-data.test.ts tests/ui/stock-kline-chart.test.tsx`
- `D:\NodeJS\npm.cmd run typecheck`
- `D:\NodeJS\npm.cmd run lint -- src/lib/results src/lib/chip tests/results tests/ui`
</verification>

<success_criteria>
- 旧结果快照字段从完整分布 latest target 派生。
- previous target 成功不会冒充 latest 筹码峰。
- 旧表格和 K 线 overlay 所需 DTO 保持兼容。
- Phase 10 未提前实现或暴露双分布图 UI。
</success_criteria>

<output>
完成后创建 `.planning/phases/10-dual-day-chip-distribution/10-03-SUMMARY.md`。
</output>
