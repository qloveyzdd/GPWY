---
phase: 08-controlled-provider-concurrency
plan: "08-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/config.ts
  - src/lib/tushare/types.ts
  - src/lib/tushare/client.ts
  - src/lib/tushare/request-scheduler.ts
  - src/lib/tushare/scheduled-client.ts
  - tests/tushare/request-scheduler.test.ts
  - tests/validation/config-boundary.test.ts
  - tests/validation/error-sanitizer.test.ts
  - .env.example
  - README.md
autonomous: true
requirements: [REFR-06, REFR-07]
must_haves:
  truths:
    - "D-01/D-02/D-03: 所有生产 provider 请求可通过一个服务进程级调度器受同一硬上限约束，默认最大并发为 8，tinyshare worker 数是硬上限内的物理容量。"
    - "D-04: 连续两次 rate_limited 后有效并发逐级下降，连续成功后缓慢恢复，且永远不超过环境变量上限。"
    - "D-05/D-06: transient 请求最多尝试 3 次，使用指数退避和随机抖动，退避期间不占用 active 槽位。"
    - "D-07/D-08: 只有 rate_limited 触发降并发；配置、token、权限、空数据和 unknown 只尝试一次。"
    - "D-15/D-16/D-18: validation > market > chip，同时等待老化和保留首次入队时间保证低优先级请求最终执行。"
    - "REST 请求超时会实际 abort fetch，而不是只让外层 Promise 提前失败。"
  artifacts:
    - path: "src/lib/tushare/request-scheduler.ts"
      provides: "全局并发、优先级老化、超时、重试、退避和动态并发策略"
      exports: ["ProviderRequestScheduler"]
    - path: "src/lib/tushare/scheduled-client.ts"
      provides: "把 TushareClientLike 请求提交到 scheduler 的适配器"
    - path: "tests/tushare/request-scheduler.test.ts"
      provides: "并发峰值、重试、动态并发和公平性确定性测试"
  key_links:
    - from: "src/lib/tushare/scheduled-client.ts"
      to: "src/lib/tushare/request-scheduler.ts"
      via: "每个 query 作为带 priority 的 schedule 任务"
    - from: "src/lib/tushare/request-scheduler.ts"
      to: "src/lib/tushare/client.ts"
      via: "每次 attempt 传递 AbortSignal"
    - from: "src/lib/config.ts"
      to: "src/lib/tushare/request-scheduler.ts"
      via: "服务端环境变量提供硬并发上限和 attempt 超时"
---

<objective>
建立 REST/tinyshare 共用的请求调度契约和可取消 REST 基础调用。

Purpose: 先把并发、重试、超时和公平性做成唯一政策层，后续 worker 与工作流只提交任务，不再各自实现重试。
Output: 服务端运行配置、query 元数据、调度器、scheduled client、REST abort 支持和核心测试。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-controlled-provider-concurrency/08-CONTEXT.md
@.planning/phases/08-controlled-provider-concurrency/08-RESEARCH.md
@.planning/phases/08-controlled-provider-concurrency/08-VALIDATION.md
@.planning/phases/08-controlled-provider-concurrency/08-PATTERNS.md
@src/lib/config.ts
@src/lib/tushare/types.ts
@src/lib/tushare/client.ts

<interfaces>
- `ProviderRequestPriority = "validation" | "market" | "chip"`。
- `TushareQueryOptions` 至少包含 `priority` 和可选 `signal`，作为 `TushareClientLike.query` 的第三参数；缺省 priority 为 `market`。
- `ProviderRequestScheduler.schedule()` 接受接口名、priority、调用方 signal 和 `execute(signal)`。
- 服务端配置使用 `TUSHARE_MAX_CONCURRENCY`（默认 8，范围 1–32）、`TUSHARE_REQUEST_TIMEOUT_MS`（默认 60000，范围 1000–300000）和 `TINYSHARE_WORKER_COUNT`（默认 2，范围 1–8）。
- scheduler 的时钟、delay 和 random 可注入；生产使用真实实现，测试使用假时钟。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义安全运行配置和 query 调度契约</name>
  <files>src/lib/config.ts, src/lib/tushare/types.ts, tests/validation/config-boundary.test.ts, .env.example, README.md</files>
  <read_first>src/lib/config.ts, src/lib/tushare/types.ts, tests/validation/config-boundary.test.ts, .env.example, README.md</read_first>
  <action>扩展服务端 Zod 配置，新增 `readProviderRuntimeConfig(env)`，解析 `TUSHARE_MAX_CONCURRENCY`、`TUSHARE_REQUEST_TIMEOUT_MS` 和 `TINYSHARE_WORKER_COUNT` 的整数范围并使用 8/60000/2 默认值；无效值应在服务启动调用处明确抛出配置错误，不能静默转成 0 或 NaN。扩展 `TushareClientLike.query` 的第三个可选参数为 `TushareQueryOptions`，包含 priority 与 signal，同时保持现有双参数 mock 兼容。更新 `.env.example` 和 README 的服务端配置说明；`SafeConfigStatus` 不返回 token、PYTHON_BIN 或原始环境变量值。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/validation/config-boundary.test.ts tests/validation/error-sanitizer.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/config.ts` 导出 `readProviderRuntimeConfig`
    - `.env.example` 包含 `TUSHARE_MAX_CONCURRENCY=8`、`TUSHARE_REQUEST_TIMEOUT_MS=60000` 和 `TINYSHARE_WORKER_COUNT=2`
    - `TushareClientLike.query` 接受可选 `TushareQueryOptions`
    - 配置状态 JSON 不包含 token、Python 路径或环境变量原始字符串
  </acceptance_criteria>
  <done>服务端拥有经过校验的并发/超时配置和不破坏现有 mock 的统一 query 元数据契约。</done>
</task>

<task type="auto">
  <name>Task 2: 实现有界调度、退避、动态并发和防饥饿</name>
  <files>src/lib/tushare/request-scheduler.ts, tests/tushare/request-scheduler.test.ts</files>
  <read_first>.planning/phases/08-controlled-provider-concurrency/08-RESEARCH.md, src/lib/tushare/client.ts, tests/refresh/fetch-refresh-data.test.ts</read_first>
  <action>新增 `ProviderRequestScheduler`。使用稳定队列项保存 base priority、首次入队时间、readyAt、序号和 attempt；priority 分值为 validation=300、market=200、chip=100，每等待 5000ms 增加 100 并封顶 300，同级按首次入队时间和序号。硬上限来自配置，effectiveConcurrency 初始等于硬上限；连续 2 次 `rate_limited` 减 1（最低 1），连续 8 次成功加 1（最高硬上限）。每次 attempt 通过 AbortSignal 实际超时；仅 `rate_limited`/`network_or_service` 重试，总尝试数固定 3。重试等待为 1000ms × 2^(attempt-1)，加入 ±20% random jitter，等待期间释放 active 槽位，重新入队时保留首次入队时间。提供只读 snapshot 供测试检查 active、queued、configured/effective concurrency。使用 Vitest fake timers 覆盖峰值、错误类别、attempt 次数、abort、降并发/恢复和持续 validation 流量下 chip 最终执行。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/tushare/request-scheduler.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 任何测试时刻观察到的底层 executor 峰值不超过 configured concurrency
    - transient 最多调用 executor 3 次，permission/empty/unknown 只调用 1 次
    - fake timer 推进前退避请求不重新执行，且其他 ready 请求可以占用空闲槽位
    - 两次连续 rate limit 后 effective concurrency 下降，八次成功后至多恢复 1
    - 持续加入 validation 请求时，等待老化后的 chip 请求仍会执行
  </acceptance_criteria>
  <done>调度器以确定性测试证明全局上限、有界退避、动态并发和公平性。</done>
</task>

<task type="auto">
  <name>Task 3: 接入 scheduled client 并让 REST 支持真实取消</name>
  <files>src/lib/tushare/scheduled-client.ts, src/lib/tushare/client.ts, tests/tushare/request-scheduler.test.ts, tests/validation/error-sanitizer.test.ts</files>
  <read_first>src/lib/tushare/client.ts, src/lib/tushare/types.ts, src/lib/tushare/request-scheduler.ts, tests/validation/error-sanitizer.test.ts</read_first>
  <action>新增 `ScheduledTushareClient`，每次 query 将 endpoint、params、priority 和调用方 signal 提交 scheduler，并只调用一次底层 client；不要在 adapter 外再循环重试。修改 REST `TushareClient` 将 options.signal 传给 fetch，捕获 AbortError/timeout 并保持 `network_or_service` 安全分类；更新 FetchLike 类型。增加测试证明 scheduler timeout 会触发底层 signal、REST fetch 收到 signal、错误摘要不包含 token 或原始 payload。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/tushare/request-scheduler.test.ts tests/validation/error-sanitizer.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `ScheduledTushareClient` 默认 priority 为 `market`
    - REST fetch init 包含 scheduler 传入的 AbortSignal
    - timeout 被 `classifyTushareError` 归类为 `network_or_service`
    - scheduled client 本身没有第二套 retry 循环
  </acceptance_criteria>
  <done>REST 已通过统一 scheduler 执行，并且单项超时能终止真实网络调用。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 环境变量 -> runtime 配置 | 非法数字可能关闭限流或制造极端资源占用。 |
| scheduler -> provider client | timeout 若未下传会产生幽灵在途请求。 |
| provider error -> retry policy | 错误误分类可能重试权限错误或泄露原始消息。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | request scheduler | mitigate | 严格整数范围、硬并发上限和峰值测试。 |
| T-08-02 | Information Disclosure | config/errors | mitigate | 仅返回安全配置状态和 `SafeTushareError`。 |
| T-08-03 | Denial of Service | timeout handling | mitigate | AbortSignal 下传并测试底层 executor 实际取消。 |
| T-08-06 | Tampering | priority metadata | accept/mitigate | priority 为内部联合类型，未知值不进入生产 API。 |
</threat_model>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/tushare/request-scheduler.test.ts tests/validation/config-boundary.test.ts tests/validation/error-sanitizer.test.ts`
- `D:\NodeJS\npm.cmd run typecheck`
- `D:\NodeJS\npm.cmd run lint -- src/lib/tushare src/lib/config.ts tests/tushare tests/validation`
</verification>

<success_criteria>
- 全局调度政策以单一模块实现，没有调用点私有并发池。
- 配置上限、attempt 上限、错误分类和优先级老化都有确定性自动测试。
- REST timeout 实际 abort 底层 fetch。
- 无新增外部依赖或客户端敏感配置暴露。
</success_criteria>

<output>
完成后创建 `.planning/phases/08-controlled-provider-concurrency/08-01-SUMMARY.md`。
</output>
