---
phase: 09-incremental-refresh-workflow
plan: "09-04"
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - src/components/status/status-workspace.tsx
  - tests/ui/status-workspace.test.tsx
  - tests/smoke/app-smoke.spec.ts
autonomous: true
requirements: [REFR-10, REFR-11, REFR-12, UI-06, UI-07]
user_setup: []
must_haves:
  truths:
    - "D-09-10/D-09-11/D-09-12: 页面显示股票列表、行情/复权、筛选、筹码处理四个阶段，包含状态、总数、完成数、失败数、耗时和脱敏错误摘要。"
    - "D-09-13: UI 约 2 秒轮询状态；筛选发布 marker 变化时刷新服务端结果，chip 终态变化时再刷新一次。"
    - "D-09-06/D-09-08/UI-06: 筹码处理 running/partial/failed 时，用户仍可查看已发布筛选结果。"
    - "D-09-14/REFR-12: 页面没有全量重建按钮、链接、菜单项或文案入口。"
  artifacts:
    - path: "src/components/status/status-workspace.tsx"
      provides: "Stage progress panel, active-work polling, result refresh triggers, safe copy"
    - path: "tests/ui/status-workspace.test.tsx"
      provides: "Component tests for stage rendering, polling, sanitized errors, result refresh markers"
    - path: "tests/smoke/app-smoke.spec.ts"
      provides: "Smoke assertion that web UI exposes manual incremental refresh but not full rebuild"
  key_links:
    - from: "src/components/status/status-workspace.tsx"
      to: "/api/refresh/status"
      via: "polling while `RefreshStatusSnapshot.hasActiveWork` is true"
    - from: "src/components/status/status-workspace.tsx"
      to: "server-rendered results snapshot"
      via: "router.refresh when resultVersion or chipVersion changes"
    - from: "src/components/status/status-workspace.tsx"
      to: "/api/refresh/run"
      via: "primary CTA starts ordinary incremental refresh only"
---

<objective>
把 Phase 9 的阶段状态契约落到页面交互：用户能看到刷新各阶段进度，并在筛选完成后立即看到结果。

Purpose: 后端增量刷新只有在页面能解释“正在做什么、哪里失败、结果是否已可用”时才真正解决用户问题。
Output: 四阶段状态面板、继续轮询后台 chip、筛选/chip marker 驱动的 router.refresh、无 full rebuild 网页入口和 UI 测试。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-incremental-refresh-workflow/09-CONTEXT.md
@.planning/phases/09-incremental-refresh-workflow/09-RESEARCH.md
@.planning/phases/09-incremental-refresh-workflow/09-UI-SPEC.md
@.planning/phases/09-incremental-refresh-workflow/09-02-SUMMARY.md
@.planning/phases/09-incremental-refresh-workflow/09-03-SUMMARY.md
@src/components/status/status-workspace.tsx
@src/lib/refresh/refresh-types.ts
@src/lib/results/results-snapshot.ts
@tests/ui/status-workspace.test.tsx
@tests/smoke/app-smoke.spec.ts

<ui_contract>
- Primary CTA label: `开始增量刷新`。
- Busy CTA label: `刷新进行中`。
- Blocked copy: `已有刷新任务正在运行，请等待当前阶段完成。`
- Stage labels: `股票列表`、`行情/复权`、`筛选`、`筹码处理`。
- No fixed 5–10 分钟 promise; show measured duration.
- No full rebuild web entry.
</ui_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 渲染四阶段进度面板和安全文案</name>
  <files>src/components/status/status-workspace.tsx, tests/ui/status-workspace.test.tsx</files>
  <read_first>src/components/status/status-workspace.tsx, src/lib/refresh/refresh-types.ts, .planning/phases/09-incremental-refresh-workflow/09-UI-SPEC.md, tests/ui/status-workspace.test.tsx</read_first>
  <action>按 UI-SPEC 在现有 header 下方新增 stage progress panel，渲染 `refreshStatus.stages` 的四个阶段。每张卡显示 label、status badge、completed/total、failed、durationMs 格式化文本；retryCount 只在 retryCount>0 且 status 为 partial/failed 时显示。running 卡使用 accent border/ring 和动画 icon；partial 使用 warning；failed 使用 destructive。错误只显示 `脱敏原因` 和 stage errorSummary，不渲染内部表名、generation id、SQLite 路径、token、headers、stack。把主按钮文案改为 `开始增量刷新`/`刷新进行中`，禁用条件改为 `refreshStatus.hasActiveWork || isStartingRefresh`。保留现有验证按钮和结果表格。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/status-workspace.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 测试能查询到四个阶段 label：股票列表、行情/复权、筛选、筹码处理
    - failed stage 显示 failed count 和脱敏摘要，但不包含 `TUSHARE_TOKEN`、`refresh.sqlite`、`market_cache_generations`
    - 主 CTA 默认文本为 `开始增量刷新`
    - chip partial/running 时 ResultsTable 仍渲染已发布结果
  </acceptance_criteria>
  <done>页面能按设计契约显示四阶段进度和安全错误文案。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 根据筛选和筹码 marker 刷新服务端快照</name>
  <files>src/components/status/status-workspace.tsx, tests/ui/status-workspace.test.tsx</files>
  <read_first>src/components/status/status-workspace.tsx, src/lib/refresh/refresh-types.ts, tests/ui/status-workspace.test.tsx</read_first>
  <behavior>
    - `hasActiveWork=true` 时每约 2 秒 poll `/api/refresh/status`，即使 `activeJob=null` 但 chip_background running 也继续 poll。
    - `resultVersion` 从 null/old 变为新值时调用一次 `router.refresh()`，满足 D-09-13。
    - `chipVersion` 变化或 chip stage 进入 terminal 状态时再调用一次 `router.refresh()`。
    - polling fetch 失败时保留最后状态，不弹出泄露内部错误的 alert。
  </behavior>
  <action>把 polling effect 的依赖从 `refreshStatus.isRunning` 改为 `refreshStatus.hasActiveWork`。新增 refs 保存上一次 `resultVersion` 和 `chipVersion`；poll 到新 status 后，如果 resultVersion 变化则 `router.refresh()`，但不要因为每次 poll 同一 marker 重复刷新。chipVersion 变化或 chip stage 从 running 转为 succeeded/partial/failed/skipped 时触发第二次 refresh。保留“刷新结束后刷新一次”的兼容逻辑，但以 marker 优先，避免 screening 发布后等待 chip 才刷新。测试使用 fake timers 模拟 refresh job succeeded + chip still running、chip terminal 两段状态。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/status-workspace.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - chip running 且 activeJob=null 时 fetch 仍每 2 秒调用
    - resultVersion 首次变化触发一次 router.refresh
    - 相同 resultVersion 重复 poll 不重复 refresh
    - chipVersion 变化触发额外一次 router.refresh
  </acceptance_criteria>
  <done>页面在筛选完成后立即刷新结果，并继续展示后台筹码进度。</done>
</task>

<task type="auto">
  <name>Task 3: 验证网页没有全量重建入口并保留关键冒烟路径</name>
  <files>tests/ui/status-workspace.test.tsx, tests/smoke/app-smoke.spec.ts</files>
  <read_first>tests/ui/status-workspace.test.tsx, tests/smoke/app-smoke.spec.ts, src/components/status/status-workspace.tsx, README.md</read_first>
  <action>增加组件测试和 smoke 断言：页面存在 `开始增量刷新`，不存在 `全量重建`、`rebuild:market`、`重建市场缓存` 等可点击网页入口。若 full_rebuild operation running，页面只显示阻塞状态文案 `全量重建正在运行，暂不能启动普通刷新。`，不显示启动 full rebuild 的按钮。保留现有登录保护、结果表格和 chart smoke 断言；不要新增 web API route 来触发 full rebuild。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/ui/status-workspace.test.tsx</automated>
    <automated>D:\NodeJS\npm.cmd run smoke -- --project=chromium tests/smoke/app-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - UI/component tests 证明没有 full rebuild 按钮、链接或菜单项
    - smoke 测试证明受保护 workspace 仍可加载并显示结果区域
    - full rebuild running 状态只阻塞普通刷新，不暴露启动入口
  </acceptance_criteria>
  <done>网页只暴露普通增量刷新，full rebuild 保持运维 CLI 能力。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/ui/status-workspace.test.tsx`
- `D:\NodeJS\npm.cmd run smoke -- --project=chromium tests/smoke/app-smoke.spec.ts`
- `D:\NodeJS\npm.cmd run typecheck`
- `D:\NodeJS\npm.cmd run lint -- src/components/status tests/ui tests/smoke`
- `D:\NodeJS\npm.cmd run verify`
</verification>

<success_criteria>
- 页面显示四阶段进度和失败数量。
- 筛选完成后不等待筹码处理即可刷新并显示结果。
- 筹码 running/partial/failed 时结果表格仍可查看。
- 页面没有全量重建入口。
- UI 符合 `09-UI-SPEC.md` 的 spacing、typography、color 和 copywriting 约束。
</success_criteria>

<output>
完成后创建 `.planning/phases/09-incremental-refresh-workflow/09-04-SUMMARY.md`。
</output>
