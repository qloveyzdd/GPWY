---
phase: 09-incremental-refresh-workflow
plan: "09-03"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/lib/refresh/bootstrap-market-data.ts
  - src/lib/refresh/full-rebuild-runner.ts
  - scripts/rebuild-market-cache.ts
  - package.json
  - package-lock.json
  - README.md
  - tests/refresh/bootstrap-market-data.test.ts
  - tests/refresh/full-rebuild-runner.test.ts
autonomous: true
requirements: [REFR-10, REFR-12]
user_setup: []
must_haves:
  truths:
    - "D-09-14/D-09-15: 维护者只能通过服务器 npm/CLI 执行全量重建，重建写入 building generation，验证通过后才激活。"
    - "D-09-16: full rebuild 与普通刷新、chip background 使用同一 operation 锁互斥。"
    - "D-09-17: CLI 输出每个阶段的状态、总数、完成数、失败数、耗时和脱敏错误摘要。"
    - "REFR-12: 网页没有全量重建入口。"
  artifacts:
    - path: "src/lib/refresh/full-rebuild-runner.ts"
      provides: "Operation-locked full rebuild orchestration"
      exports: ["runFullMarketRebuild"]
    - path: "scripts/rebuild-market-cache.ts"
      provides: "Server shell entrypoint for full market cache rebuild"
    - path: "package.json"
      provides: "`rebuild:market` npm script"
  key_links:
    - from: "scripts/rebuild-market-cache.ts"
      to: "src/lib/refresh/full-rebuild-runner.ts"
      via: "CLI parses options, creates Tushare client, prints stage snapshots from runner"
    - from: "src/lib/refresh/full-rebuild-runner.ts"
      to: "src/lib/refresh/bootstrap-market-data.ts"
      via: "full rebuild reuses building-generation bootstrap and activation validation"
    - from: "src/lib/refresh/full-rebuild-runner.ts"
      to: "src/lib/refresh/refresh-store.ts"
      via: "full rebuild acquires `full_rebuild` operation lock before provider work"
---

<objective>
提供运维专用全量重建命令，并保持 building generation 原子激活语义。

Purpose: 用户已经确认全量交易数据/重建可能按小时计算，因此该能力必须从网页移除，仅作为服务器 shell 运维路径，并且失败时不破坏 active cache 与旧结果。
Output: full rebuild runner、npm CLI、阶段输出、README 运维说明和测试。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-incremental-refresh-workflow/09-CONTEXT.md
@.planning/phases/09-incremental-refresh-workflow/09-RESEARCH.md
@.planning/phases/09-incremental-refresh-workflow/09-VALIDATION.md
@.planning/phases/09-incremental-refresh-workflow/09-01-SUMMARY.md
@src/lib/refresh/bootstrap-market-data.ts
@src/lib/refresh/refresh-store.ts
@src/lib/refresh/market-data-store.ts
@tests/refresh/bootstrap-market-data.test.ts
@package.json
@README.md

<interfaces>
- `runFullMarketRebuild({ client, now, targetTradingDates?, maxLookbackDays?, onStage? })` 获取 `full_rebuild` operation 锁，调用 bootstrap，输出 stage snapshots。
- npm script 名称固定为 `rebuild:market`。
- CLI 支持 `--help` 输出用法且不触发 provider 请求。
- 如果需要 TypeScript CLI runner，使用 `tsx` 并把 package-lock 同步提交；不要依赖全局工具。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 提取 full rebuild runner 并复用 bootstrap 原子激活</name>
  <files>src/lib/refresh/bootstrap-market-data.ts, src/lib/refresh/full-rebuild-runner.ts, tests/refresh/bootstrap-market-data.test.ts, tests/refresh/full-rebuild-runner.test.ts</files>
  <read_first>src/lib/refresh/bootstrap-market-data.ts, src/lib/refresh/market-data-store.ts, src/lib/refresh/refresh-store.ts, tests/refresh/bootstrap-market-data.test.ts</read_first>
  <behavior>
    - full rebuild 成功时创建新的 building generation，完整验证后激活，旧 active generation retired。
    - provider 或 store 失败时删除 building generation，旧 active generation 仍 active，满足 D-09-15。
    - 有任何 active operation 时 full rebuild started=false 或抛出稳定 `refresh_operation_already_running`，满足 D-09-16。
    - runner 输出 `股票列表`、`行情/复权`、`筛选` 之外至少 `stock_list` 和 `market_data` 的阶段状态；chip 不由 full rebuild 启动。
  </behavior>
  <action>新增 `full-rebuild-runner.ts`，先调用 `startRefreshOperation("full_rebuild")` 获取锁，再创建 provider client 或使用注入 client，复用 `bootstrapMarketData` 完成股票、目标日期、daily 和 adj_factor 的 building-generation 写入与激活。为 `bootstrapMarketData` 增加可选 progress callbacks，报告 stock list 开始/完成、trade date total、每个 daily/factor item 的完成/失败；保持原有 bootstrap tests 通过且不改变 activation validation。失败路径必须等待 bootstrap allSettled/cleanup 完成后再 fail operation。runner 返回最终 operation snapshot 或抛出脱敏错误；不要写 refresh_jobs，也不要启动 screening/chip。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/bootstrap-market-data.test.ts tests/refresh/full-rebuild-runner.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - full rebuild 失败测试中旧 active generation id 不变
    - full rebuild 与手动 refresh/chip operation 互斥测试通过
    - bootstrap 旧测试仍证明 failed building generation 被清理
    - progress callback 不改变 provider 请求并发上限
  </acceptance_criteria>
  <done>全量重建拥有可测试的运维 runner，并保留 Phase 7 的原子激活安全边界。</done>
</task>

<task type="auto">
  <name>Task 2: 添加 npm CLI、依赖锁和运维说明</name>
  <files>scripts/rebuild-market-cache.ts, package.json, package-lock.json, README.md, tests/refresh/full-rebuild-runner.test.ts</files>
  <read_first>package.json, README.md, src/lib/config.ts, src/lib/tushare/provider.ts, src/lib/refresh/full-rebuild-runner.ts</read_first>
  <action>新增 `scripts/rebuild-market-cache.ts`。CLI 从现有 `readTushareTokenSecret` 和 `createTushareClient` 获取 provider，不从命令行接收 token；`--help` 打印用法和风险说明；正常执行时调用 `runFullMarketRebuild` 并按 D-09-17 输出阶段 label、status、total、completed、failed、durationMs、retryCount、errorSummary。输出可以是稳定 JSON lines 或紧凑表格，但必须不包含 token、本地 SQLite 路径、headers 或原始 provider payload。给 `package.json` 增加 `rebuild:market` script；如果当前 Node 不能直接执行 TS 脚本，则添加 `tsx` 到 devDependencies 并同步 `package-lock.json`。README 增加“运维全量重建”小节，说明此命令只能在服务器 shell 执行、可能按小时运行、失败时旧结果继续可用、网页没有入口。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/full-rebuild-runner.test.ts tests/refresh/bootstrap-market-data.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
    <automated>D:\NodeJS\npm.cmd run rebuild:market -- --help</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` 包含 `rebuild:market`
    - `npm run rebuild:market -- --help` 不需要 TUSHARE_TOKEN 且退出成功
    - README 说明 full rebuild 是服务器 shell 运维路径，不是网页功能
    - CLI 输出不包含 `TUSHARE_TOKEN`、`REFRESH_DB_PATH`、Windows/Unix 本地路径或 headers
  </acceptance_criteria>
  <done>维护者可以通过 npm 命令执行全量重建，并能看到脱敏阶段输出。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/refresh/full-rebuild-runner.test.ts tests/refresh/bootstrap-market-data.test.ts`
- `D:\NodeJS\npm.cmd run rebuild:market -- --help`
- `D:\NodeJS\npm.cmd run typecheck`
- `D:\NodeJS\npm.cmd run lint -- src/lib/refresh scripts tests/refresh`
</verification>

<success_criteria>
- 全量重建只通过 `npm run rebuild:market` 暴露。
- 失败时旧 active cache 和旧筛选结果继续可用。
- CLI 输出阶段状态和脱敏失败摘要。
- 网页没有 full rebuild 入口。
</success_criteria>

<output>
完成后创建 `.planning/phases/09-incremental-refresh-workflow/09-03-SUMMARY.md`。
</output>
