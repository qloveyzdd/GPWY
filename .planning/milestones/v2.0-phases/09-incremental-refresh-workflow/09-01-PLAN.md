---
phase: 09-incremental-refresh-workflow
plan: "09-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/refresh/refresh-types.ts
  - src/lib/refresh/refresh-store.ts
  - src/lib/refresh/market-data-types.ts
  - src/lib/refresh/market-data-store.ts
  - tests/refresh/refresh-store.test.ts
  - tests/refresh/market-data-store.test.ts
autonomous: true
requirements: [DATA-07, REFR-09, REFR-10, UI-07]
user_setup: []
must_haves:
  truths:
    - "D-09-01/D-09-02/D-09-03/D-09-04: 系统能基于 active generation 与目标 60 个交易日计算 daily 与 adj_factor 的独立缺失/失败数据项。"
    - "D-09-09/D-09-16: 任一普通刷新、筹码后台阶段或全量重建运行中时，新的运行会被同一个 SQLite 运行锁拒绝。"
    - "D-09-10/D-09-11/D-09-12: 刷新状态快照可以返回四个阶段的状态、总数、完成数、失败数、耗时、重试数和脱敏错误摘要。"
  artifacts:
    - path: "src/lib/refresh/refresh-types.ts"
      provides: "Refresh operation, stage progress, result marker, and active-work DTO types"
      contains: "RefreshStageSnapshot"
    - path: "src/lib/refresh/refresh-store.ts"
      provides: "SQLite refresh operation lock and stage progress persistence"
      exports: ["startRefreshOperation", "completeRefreshOperation", "upsertRefreshStage", "readRefreshOperationSnapshot"]
    - path: "src/lib/refresh/market-data-store.ts"
      provides: "Active generation target-date gap planning and readiness helpers"
      exports: ["planActiveGenerationMarketWork", "assertActiveGenerationReadyForScreening"]
  key_links:
    - from: "src/lib/refresh/refresh-store.ts"
      to: "src/lib/refresh/refresh-types.ts"
      via: "store functions return `RefreshOperationSnapshot` and `RefreshStageSnapshot` DTOs"
    - from: "src/lib/refresh/market-data-store.ts"
      to: "src/lib/refresh/market-data-types.ts"
      via: "gap planner returns item-level daily/factor work typed as market data work items"
    - from: "tests/refresh/market-data-store.test.ts"
      to: "src/lib/refresh/market-data-store.ts"
      via: "tests prove missing daily and missing factor are planned independently"
---

<objective>
建立 Phase 9 的共享数据契约：阶段进度、互斥运行锁、active generation 增量差集和筛选前完整性检查。

Purpose: 后续普通刷新、后台筹码、全量重建和 UI 都依赖同一状态模型；先把可恢复与可解释的事实边界固定下来，避免在 runner/UI 中各自拼状态。
Output: 刷新 operation/stage 持久化 API、active generation 数据项差集规划 API、完整性断言 API 和对应单元测试。
</objective>

<execution_context>
@C:/Users/Qlovey/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Qlovey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-incremental-refresh-workflow/09-CONTEXT.md
@.planning/phases/09-incremental-refresh-workflow/09-RESEARCH.md
@.planning/phases/09-incremental-refresh-workflow/09-UI-SPEC.md
@.planning/phases/09-incremental-refresh-workflow/09-VALIDATION.md
@src/lib/refresh/refresh-types.ts
@src/lib/refresh/refresh-store.ts
@src/lib/refresh/market-data-types.ts
@src/lib/refresh/market-data-store.ts
@tests/refresh/refresh-store.test.ts
@tests/refresh/market-data-store.test.ts

<interfaces>
- `RefreshOperationKind = "manual_refresh" | "chip_background" | "full_rebuild"`。
- `RefreshStageKey = "stock_list" | "market_data" | "screening" | "chip"`，UI label 固定为 `股票列表`、`行情/复权`、`筛选`、`筹码处理`。
- `RefreshStageStatus = "pending" | "running" | "succeeded" | "partial" | "failed" | "skipped"`。
- `RefreshStageSnapshot` 至少包含 stage、label、status、total、completed、failed、retryCount、startedAt、finishedAt、durationMs、errorSummary。
- `RefreshStatusSnapshot` 保留现有 activeJob/latestJob/latestSuccessfulJob/latestCacheStats 字段，同时新增 activeOperation、stages、hasActiveWork、resultVersion、chipVersion。
- `planActiveGenerationMarketWork(generationId, targetTradeDates)` 返回缺失或 failed 的 item-level work，不返回已 succeeded 的项目。
- `assertActiveGenerationReadyForScreening(generationId, targetTradeDates)` 只在目标 60 日 daily 与 factor 均 succeeded 时通过。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 持久化刷新 operation 锁与阶段进度</name>
  <files>src/lib/refresh/refresh-types.ts, src/lib/refresh/refresh-store.ts, tests/refresh/refresh-store.test.ts</files>
  <read_first>src/lib/refresh/refresh-types.ts, src/lib/refresh/refresh-store.ts, tests/refresh/refresh-store.test.ts, .planning/phases/09-incremental-refresh-workflow/09-UI-SPEC.md</read_first>
  <behavior>
    - 当 `startRefreshOperation("manual_refresh")` 成功后，`readRefreshOperationSnapshot()` 返回 hasActiveWork=true、activeOperation.kind="manual_refresh"。
    - 当已有 `manual_refresh`、`chip_background` 或 `full_rebuild` running operation 时，再次 `startRefreshOperation(...)` 返回 started=false 并指向现有 operation，满足 D-09-09 和 D-09-16。
    - `upsertRefreshStage` 可为四个 stage 写入 pending/running/succeeded/partial/failed/skipped，读取时按 UI-SPEC 顺序返回。
    - errorSummary 写入前必须经过调用方脱敏；store 只保存传入的摘要，不生成或暴露 token、路径、headers。
  </behavior>
  <action>在 `refresh-store.ts` 增加 `refresh_operations` 和 `refresh_operation_stages` 的 additive migration。`refresh_operations` 字段包含 id、kind、status、started_at、finished_at、owner_refresh_job_id、error_summary，并增加 partial unique index `refresh_operations_one_running` 约束 status='running'。`refresh_operation_stages` 以 `(operation_id, stage_key)` 为主键，保存 status、total_count、completed_count、failed_count、retry_count、started_at、finished_at、error_summary。导出 `startRefreshOperation(kind, { ownerRefreshJobId?, now? })`、`completeRefreshOperation`、`failRefreshOperation`、`upsertRefreshStage`、`readRefreshOperationSnapshot`。扩展 `refresh-types.ts`，但保持旧字段兼容，使现有 UI 和 API 编译不破。阶段 label 必须使用 D-09-10 锁定的中文名；durationMs 由 startedAt/finishedAt 或当前时间计算，retryCount 允许为 0。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/refresh-store.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `refresh_operations_one_running` 存在并阻止任意两类 operation 并发 running
    - `readRefreshOperationSnapshot()` 返回四个固定 stage，缺失 stage 补 pending 默认值
    - `RefreshStatusSnapshot` 仍包含 `isRunning`，并新增 `hasActiveWork`
    - 阶段 snapshot 不包含 generation id、SQLite 路径、token 或 provider 原始 payload 字段
  </acceptance_criteria>
  <done>刷新、后台筹码和全量重建拥有统一互斥锁与可展示阶段状态。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 规划 active generation 的缺失/失败数据项</name>
  <files>src/lib/refresh/market-data-types.ts, src/lib/refresh/market-data-store.ts, tests/refresh/market-data-store.test.ts</files>
  <read_first>src/lib/refresh/market-data-types.ts, src/lib/refresh/market-data-store.ts, tests/refresh/market-data-store.test.ts, .planning/phases/07-standardized-market-data-cache/07-03-SUMMARY.md</read_first>
  <behavior>
    - 目标日期不存在 manifest 行时，planner 产生 daily 和 factor 两个 work items。
    - 目标日期 daily=succeeded、factor=failed 时，只产生 factor work item，满足 D-09-02。
    - 目标 60 日均 paired succeeded 时，planner 返回空 work，满足 D-09-03 和 REFR-13。
    - 目标 60 日未完整时，`assertActiveGenerationReadyForScreening` 抛出稳定错误 `active_generation_target_incomplete`。
  </behavior>
  <action>在 `market-data-types.ts` 增加 `MarketDataItemKind = "daily" | "factor"`、`MarketDataWorkItem`、`ActiveGenerationMarketWorkPlan` 等类型。在 `market-data-store.ts` 导出 `planActiveGenerationMarketWork(generationId, targetTradeDates)`，只比较传入的当前目标 60 个交易日；不要使用 generation 全量历史日期推断目标窗口。导出 `ensureMarketGenerationDates(generationId, tradeDates, now?)` 为缺失日期创建 pending/pending manifest 行；导出 `updateMarketGenerationDateItemStatus(generationId, tradeDate, itemKind, status, now?)`，更新单个 item 时保留另一个 item 的当前状态。导出 `readPairedSuccessTradeDates(generationId, targetTradeDates)` 和 `assertActiveGenerationReadyForScreening(generationId, targetTradeDates)`；active generation 可以拥有超过 60 个历史 manifest 行，但 readiness 只看当前目标 60 日。测试覆盖 missing date、daily failed、factor failed、paired success、active 旧数据保留和 building generation 校验不被放宽。</action>
  <verify>
    <automated>D:\NodeJS\npm.cmd run test -- --run tests/refresh/market-data-store.test.ts</automated>
    <automated>D:\NodeJS\npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `planActiveGenerationMarketWork` 不返回任何 succeeded/succeeded 日期的数据项
    - 单项状态更新不会把另一个 item 从 failed/pending 覆盖成 succeeded
    - readiness 失败不会修改 active generation 或删除任何已成功数据
    - `validateMarketCacheGeneration` 仍只把完整 building generation 判定为可激活
  </acceptance_criteria>
  <done>普通刷新可以确定性知道只补哪些 daily/factor 项，并能在筛选前阻止不完整窗口。</done>
</task>

</tasks>

<verification>
- `D:\NodeJS\npm.cmd run test -- --run tests/refresh/refresh-store.test.ts tests/refresh/market-data-store.test.ts`
- `D:\NodeJS\npm.cmd run typecheck`
- `D:\NodeJS\npm.cmd run lint -- src/lib/refresh tests/refresh`
</verification>

<success_criteria>
- 统一 operation 锁可以阻止普通刷新、筹码后台和全量重建并发。
- 阶段进度 DTO 满足 UI-SPEC 的四阶段字段要求。
- active generation 增量 work planning 按 daily/factor 独立续跑，且不会规划已完成项目。
- 筛选前完整性断言只允许当前目标 60 日 paired success。
</success_criteria>

<output>
完成后创建 `.planning/phases/09-incremental-refresh-workflow/09-01-SUMMARY.md`。
</output>
