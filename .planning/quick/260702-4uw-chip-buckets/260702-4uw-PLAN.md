# Quick Task 260702-4uw: 修复筹码分布图细价位标签覆盖导致最大占比和柱子不一致

## 目标

修复计算筹码分布中多个 4 位小数价位显示成同一个两位小数价位时，柱状图数据被覆盖、导致“最大占比”和实际柱长不一致的问题。

## 范围

- 仅修改前端图表展示层。
- 保留后端 DTO、缓存和计算模型的原始细粒度 levels。
- 图表展示按两位小数价位桶聚合 percent，并用聚合后的数据计算 y 轴、x 轴最大值和最大占比标注。

## 验证

- 增加组件回归测试覆盖同一显示价位桶内多个细价位聚合。
- 运行 `npm run verify`。
